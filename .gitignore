*.o
*.bin

# エディタのバックアップ/一時ファイル
\#*\#
*~

# ビルド成果物（de-nest 後は repo root 直下）
boot/x86/kernel.elf
boot/x86/pknl.raw
boot/x86/disk.img
boot/x86/serial.log
boot/aarch64/kernel.elf
test_results.log

# Claude Code tool state (subagent worktrees etc.) — never commit
.claude/

# NS-1 student: teacher byte-targets are harvested at runtime (run_student.sh) —
# never commit the generated fixture blob
tests/llm/*.bytes

# userland sample ELF — built from samples/ by userland/x86/Makefile (make disk)
userland/x86/**/*.elf

# galaxy v1 (galaxy.md D3): build-time generated page array (regenerated
# from arch/common/web/galaxy.html by tools/galaxy/gen_page.py; never commit)
boot/linux/galaxy_page.h
boot/linux_x86_64/galaxy_page.h
# ark-profile v1 (ark-profile.md §7.1): build-time generated manifesto array
# (regenerated from arch/common/web/manifesto.txt; never commit)
boot/linux/manifesto_page.h
boot/linux_x86_64/manifesto_page.h
boot/aarch64/manifesto_page.h
boot/x86/manifesto_page.h

# gcc -MMD が生成するヘッダ依存ファイル
*.d
