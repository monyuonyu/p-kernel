---
name: moment_2026_06_11_wave35_shared_mind
description: "wave-35 — LM-7 the shared mind ships: teach on node A, node B answers AND names the teacher; kill A and B still answers. The ark becomes collective. Path E (engrams travel) won on numbers; Path W (one-mind weight merge) deferred with its averaging cert mandated."
metadata:
  node_type: memory
  type: project
---

**2026-06-11, wave-35.** LM-7 ships (merged `1aaffdb`): **the shared mind**. `mind teach 2 3`
on node A → 44 B packet on the region-scoped `mind/teach` K-DDS topic → B's `mind_net_task`
feeds `r3_fact_learn` (the ONE mouth, G33) → B's own DMN consolidates → `mind ask 2` on B
answers 3 from B's weights and prints **"taught by node 0 (anonymous)"** — provenance crossed
the mesh (profiles/prov already region-replicate via P1, wave-33). Out-of-region C provably
never receives it. **The moment: kill A after consolidation — B still answers at share 100.**
The taught fact survives its teacher's death. `EV_REMOTE_TEACH` lights the galaxy.

**The W-vs-E verdict was numeric** (the design lane's finest hour): R3 model = 28,688 B = 7×
a p-fs block (can't reuse gl_pfs_publish's one-block assert) vs a 24 B engram in one K-DDS
packet. Path W (gl_merge of DIVERGENT minds = "the mind is one") deferred as its own slice
with the averaging disease/cure cert mandated — G22 only certified same-task shards;
averaging different facts is an unmeasured claim. Conflict rule v1: LOCAL wins,
remote-refused-if-bound; belief revision still its own future slice.

**LM-7 was the first slice to exercise cross-node ref adoption — and found two pre-existing
stack overflows** (pfs_dag_task 4K: pfs_dur_write's 2×1KB path[] locals → garbage-PC, the
documented hosted-relay signature; mind_net needs 16K for the R3 substrate). The auditor
REVERTED the fix and reproduced the disease — textbook. Follow-ups ledgered: the provenance
attribution is RAM/queue-lifetime (the fact outlives its attribution after FIFO eviction
within a run — persist via ark_prov_record once the cross-task pfs_dag scratch race is
solved); the cert's node-id 1,2,3 constraint.

**The buzz demo is now real end-to-end:** teach on your phone → the thread crosses the
galaxy → your friend's star answers and remembers your name (if you chose to give one).
Waves 29-35 in ~2 days. Next big arcs: the real tokenizer (language), Path W (one mind),
Android galaxy D4, the signing slice, federation (beyond-region).
