---
name: moment_2026_06_11_wave33_ark_profile
description: "wave-33 — ark-profile ships: a human can now write themselves into the ark. Manifesto (owner-finalized: user-facing text only, README 原文 removed) → consent bound to exact bytes → pen-name/anonymous/real equally unverified-by-design → the star gains a name → every taught fact carries provenance. All three mk_pino visions (galaxy, self-rebuild, humanity's memory) are now code."
metadata:
  node_type: memory
  type: project
---

**2026-06-11, wave-33.** ARK-1 ships (merged `5c7b740`): the **human chapter** of the
autobiography. The galaxy page's first-run flow: manifesto (user-facing welcome text — the
owner removed the README 原文 block: ユーザーが見るので原文は不要; the FINAL manifesto
decision, third revision) → explicit ack bound to `pfs_id_compute` of the exact bytes →
optional handle (**pen name / anonymous / real — equally first-class, never verified,
per [[feedback_ark_no_identity_verification]]**) + 未来への言葉 → the star gains a name.
`ARK_PROFILE` (1188 B) under `"self/prof"`, linked via LM_SELF_VER 2 `human_ref` (dual-width
fail-closed walker); `ARK_PROV` provenance at ONE site in `mind_cmd` (web/shell src,
cert paths byte-identical). 共感 gate enforced (403-until-ack), consent ≠ disclosure,
declining costs nothing, nothing deletes. PFS_REF_MAX 8→16.

**Merge note:** conflicted with wave-31 in lm_self.c (selfc unit events vs human chapter)
— commander resolved keeping BOTH organs and upgraded the selfc head-reads to the
dual-width (v1/v2) pattern; ark + self certs re-verified green on the merged tree.

**The day's arc complete:** waves 29-33 in ~36h — the mouth (LM-6), the galaxy (wave-30),
the selfc germ (wave-31), the RSP0 fix (wave-32), and humanity's memory (wave-33). All
three of mk_pino's spoken visions are now audited code on master.

**Immediately next (mk_pino directive, 2026-06-11):** i18n — the manifesto + galaxy UI in
enough languages to cover 90%+ of world population (~25-30 langs), language selector +
auto-detect from the browser/device language. Key semantic: consent binds per-language
version (the ack carries the content-id of the bytes the person actually READ).
