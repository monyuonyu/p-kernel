---
name: moment-2026-06-12-waves42-44
description: "wave-42 Path W² Fisher merge WEIGHTED-WINS (8.8%→85%), wave-43 signing gates fleet evolution, wave-44 KILL-CHURN honest negative (~42% unchanged, 3 theories refuted)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 2563c3bf-6291-45c2-a7f8-68ef86d738f8
---

# 2026-06-12 — the second 全方向 batch lands (waves 42/43/44)

Three parallel lanes (all model:opus implementers + SEPARATE auditors), merged same day:

**Wave-42 — LM-11 / Path W² (Lane C).** The Fisher-weighted merge BEATS the plain
mean on divergent minds: plain mean k2=8.8% (≈chance), weighted k2=**85.0%**, k1
stays 100% — verdict `WEIGHTED-WINS`, NO replay needed. `r3_fisher_diag` = Σrg²
through the existing `r_backward`; `gl_merge_w`/`gl_accumulate_w` are siblings,
`gl_merge` itself UNCHANGED. The real engineering was the **floor**: absolute 1e-6
drowned the signal (artifact TIE); shipped floor is RELATIVE (`WM_FLOOR_FRAC=1e-3
× max(fisher)`), auditor swept 4 decades — wins everywhere. W² = cheap first-line
merge; union replay (LM-10) remains the deep sleep.

**Wave-43 — SIGN-2, signing in the live fleet (Lane A).** SIGN_MANIFEST (144B)
travels with self-built units; `sign_manifest_verify` (sign.c:167) ANDs
magic/version + artifact_id RECOMPUTED from actual bytes (body-swap refused live
by the audit) + Ed25519 verify. `selfc_run_germ` (selfc.c:534) = local-allowlist
OR fleet-signed; the ONLY trust anchor is the operator typing `selfc adopt key
<hex>` locally. This discharges SELFC-R3's "LOCAL-ONLY until signing" — networked
self-built code is no longer a malware mesh. Boundary held: keys = NODES, never
humans ([[feedback-ark-no-identity-verification]]).

**Wave-44 — KILL-CHURN continuation (Lane B): HONEST NEGATIVE.** ~42% crash rate
UNCHANGED; refuted three theories empirically incl. the predecessor's own
"imalloc-above-16MB ROOT FIX". Shipped hardening (poison-on-free, ctxtsk guard)
+ a deterministic `dproc churn` reproducer verb. Best evidence now: use-after-free
of a RECYCLED daemon TCB, `tskque` corrupt when `knl_wait_release_tmout` fires;
prime suspect `dproc_kill_by_name` killing a stale tid. Row stays OPEN — the
ledger's bar was not weakened. ([[feedback-audit-is-the-engine]])

Ops notes: Lane B's first agent was server-side rate-limited at 163 tool uses —
checkpoint-commit + continuation agent worked. Two malformed tool dispatches by
the commander were caught and re-issued. All three merges were conflict-free;
all 3 build targets (linux aarch64, linux x86_64, bare x86) verified post-merge.
