---
name: moment_2026_06_12_wave41_one_mind
description: "wave-41 — Path W / the one mind: MEASURED, not asserted. Naive weight-averaging of two minds that learned DIFFERENT facts is LOSSY (one fact survives 100%, the other collapses to ~chance 8.8%); union-replay consolidation after the merge recovers BOTH to 100%. So 心をひとつに is real, but needs collective sleep, not just the average. The commander's own prior (preservation) was WRONG — the measurement corrected it."
metadata:
  node_type: memory
  type: project
---

**2026-06-12, wave-41.** mk_pino: 「Path Wいきましょう、心をひとつに」. The slice LM-7 deferred
(VIII.7, "averaging cert mandated") is now MEASURED. This was a genuine experiment — the
outcome existed nowhere until it ran; Claude's stated prior (sparse facts → preservation) was
WRONG, and the number corrected it. A model case of "intelligence ≠ knowing the outcome;
you have to run it."

**The result (audited, reproduced on the auditor's own multi-node rebuild):**
- `[onemind-divergent]` — teach k1@node0, k2@node1 (same seed, DIFFERENT facts); each solo 100%.
  Naive `gl_merge` (no replay): **k1=100%, k2=8.8% (chance 1.6%)** → (c) PARTIAL. **Plain
  averaging of divergent minds is LOSSY — one fact survives, one dies.** WHY: the surviving
  value appears 3× in the SDICT prior, clearing the halved-gradient margin (stable, not seed).
- `[onemind-cured]` — CURE A (each replays its own then re-merge) FAILS honestly (41/49,
  oscillation). **CURE B = UNION interleaved replay of both retained engrams on the MERGED rw[]
  via s_round(1)** (LM-5 fleet-wide, no new math, held-out S_SEED_HELD masked eval — auditor
  confirmed NOT leakage/re-teach) → **both facts 100%, k2 gain +91.3%.** The mind is one at the
  substrate, to the measured accuracy.
- `[onemind-nocentral]` |fwd−rev|=0 at n=R_NP=21568; `[onemind-survive]` — B folds A's real
  84KB (22 K-DDS chunks, all-or-nothing) via gl_merge at n=R_NP, kill A, B answers both at 100%.

**Framing shipped:** Path W = the fleet's DMN (weight-merge = collective SLEEP); Path E = the
fast synapse. Complementary. Cost OWNED + PRINTED: 86272 B/peer/round (~1900× Path E). Merged
`70d68d7`, epitaph wave-41 (`858ac4d`). gl_merge reused at n=R_NP (uncapped math; GL_MAXFLOATS
caps only the blob transport); rw[] single; buffers static; LM-9 byte-identical.

**Honest bounds (ledgered):** `[onemind-survive]`'s ANSWER isn't isolated from co-running Path E
(the FOLD mechanism is proven; attribution isn't) — future: disable E during the test. The
live fold inline-reimplements gl_merge (math-identical) — collapse to the one averager.

**The connection mk_pino found:** this is FedAvg (Google Gboard, on-device LM, since 2017) —
but the EASY case is same-task + central server; Path W is the HARD case (different facts +
NO central) that FedAvg's success doesn't answer. Behind on ML scale, alone on the setting.
