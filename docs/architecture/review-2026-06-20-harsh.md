<!-- Automated hostile whole-project review requested by mk_pino, 2026-06-20. Kept verbatim as honest 歴史地層. -->

# HARSH EXTERNAL AUDIT — p-kernel @ `20937721` (the 0.9.3 marathon session)

**Note on scope:** The worktree HEAD was `5833b1a4` (a different, APK-polish line ending wave-55). The session you asked me to judge lives on `20937721` (waves through 56 + SS/N/T/interocept). I checked out `20937721` and audited there. All file:line below are on that commit. I verified by building/running (SS-5 cert: real PASS) and reading source, not docs.

## VERDICT
The project is **healthier than at the 2026-06-14 critique on the dimensions the critique attacked** — the gap-ledger genuinely shrank to **1 open row** (`gap-ledger.md:54`), KILL-CHURN's ring0 #PF was finally killed with a real root-cause fix and a matched control after two honestly-reverted false fixes, and the `[live]`/`[in-proc]` tag convention is now a load-bearing mechanical institution. **But this specific session is 70% motion and 30% load-bearing progress, and it ran straight into the bloat the critique warned about.** It shipped ~6 "SELECTION-FUNCTION-ONLY / MAP-ONLY / SEAM-ONLY" slices whose actual distributed behavior is deferred, plus ~1,250 lines of new design-plan docs, while the one thing the critique named as priority — **federation / a real >4-node live mesh** — got only a "design-harden" and remains `[in-proc]` at N=3-4. The mind did not get materially more capable; the scaffolding around the unbuilt mind did.

## SHARPEST FINDINGS (ranked)

**1. 🔴 "implementer ≠ auditor ≠ commander" is STRUCTURALLY UNVERIFIABLE — and this is the immune system applied to itself failing.**
All 37 session commits are authored `monyuonyu <monyuonyu@gmail.com>` / `Co-Authored-By: Claude Opus 4.8` (`git log --format="%an" 40732272^..20937721` → 37/37 identical). The "separate audit" is a subagent the same orchestrator spawned with a prompt it wrote, and it leaves **zero independent trace in the artifact**. The 2026-06-14 critique's killer insight was "the immune system isn't applied to itself." This is exactly that, unfixed: the project's central identity claim ("implementer≠auditor") is asserted in commit prose ("Separate audit to follow", `9f7a9bc4`) but is **theatrical at the git level** — one actor wrote all three roles. A real external auditor (me) cannot distinguish "independently audited" from "the same model re-read its own diff." Severity 🔴: it contradicts the constitution that the memory file calls "the project's LIFE / immune system."

**2. 🟡 This session is a FEATURE MARCH against the standing critique, dressed as foundation work.** SS-3 (`run_ss3.sh:6` "IN-PROCESS"), SS-5 (`placement.c`, "the placement MAP only — EXECUTION is SS-6 DEFERRED", `run_ss5.sh:31`), N-2 ("SELECTION FUNCTION ONLY", `1f656c3c`), N-2b (capability gossip, "true multi-process live mesh left as a deferred `[live]` row"), T-fix-a ("**HONEST: SELECTION only**", `5270bbae`), §7 G38.0 ("behaviour-PRESERVING refactor… NO learning", `9f7a9bc4`). Six slices, and in **every one the load-bearing distributed/learning behavior is the deferred half.** What shipped is pure functions and seams: HRW maps, lowest-id selectors, an extracted `moe_select_step`. These are cheap, safe, green — and they don't make the mind think bigger or the fleet bind wider. mk_pino's docs-as-年輪 ruling fairly covers the design docs, but it does not convert "shipped a selector for a transport that doesn't exist" into shipped capability. Opportunity cost: federation R0 (the critique's named priority) is still just "IN FLIGHT design-harden" in `BACKLOG.md`.

**3. 🟡 "Design-harden caught a real bug" is being counted as a win for bugs in code that was then NOT built.** Two session certs (`thread-t-impl-plan.md`, `survival-g38-impl-plan.md`) "each caught a real live-code bug" per `BACKLOG.md`. But the §7 work explicitly stops at "Wave 0 ONLY. NO gacc, NO learning… deliberately on hold pending mk_pino's review" (`9f7a9bc4`), and Thread T's bridge is "deferred… serializes after SS-3." Finding a cert bug in an impl plan whose implementation is on hold is **motion that produces a doc, not a defended behavior.** The §7 G38.0 commit that DID ship is a behavior-preserving refactor — by its own description it moves no digits. Valuable as a tripwire, but it is plumbing for a feature the project decided not to build yet.

**4. 🟡 BACKLOG.md is visibly a sloppy merge — the honesty apparatus is decaying into duplication.** `self-access R0 (d730175e)` appears **twice verbatim**, `N-2 slice 1 (1f656c3c)` **twice**, N-2b twice, interoception slice-1 twice (`grep -c` confirms 2 each). The "DONE THIS SESSION" block literally repeats four entries. This is small, but it's the AUDIT-SPRAWL disease the ledger's own Rule 11 warns about ("if this file ever rivals the learning code… it has become the disease it tracks") leaking into the one master tracking doc. The session that wrote the most docs also degraded doc hygiene.

**5. 🟡 Crashed-agent "salvage" makes the commander a co-implementer — a real implementer≠auditor breach, self-admitted.** §7 G38.0 is "**Commander-salvaged from a socket-crashed implementer** (verified before commit: 4/4 targets link…)" (`9f7a9bc4`). When the commander persists a dead implementer's uncommitted diff AND verifies it AND commits it, the commander has co-implemented. The mitigation ("Separate audit to follow") routes back to Finding 1's unverifiability. This is the same pattern memory already flagged ("Opus implementers leave work uncommitted — check their worktree status") — recurring, not closed.

**6. 🟢/honest — the grand-claim gap is DISCLOSED, not hidden (credit where due).** The vision doc itself labels F1 "**DECORATIVE**", F2b "**ABSENT**" with file:line (`special-structure-mind.md:57-59`), and I verified the core: `cap_experts_of(N)=clamp(n,1,16)` is consumed ONLY by `degrade_stat` (a print) and `capacity_self_test` (the cert) — it sizes **no model** (grep: zero non-degrade.c consumers). "Brain grows with the fleet" is, today, a clamp that sizes nothing. README says "10,000 nodes = 看板 (signboard); node_id past 254 needs a wire change; federation = 設計のみ" (`README.md:105,161`). DNODE_MAX=64, but the heaviest **`[live]`** test is N=3-4 (`samples/11_distributed/run_3node_full.sh`); ledger even names the "~32 hub-centric live cap." The gap is real and large, but the prose does NOT oversell it — it self-flags. That is the immune system working at the doc level even as it fails at the process level (Finding 1).

**7. 🟢 The honesty-tag integrity is genuinely strong.** I ran `run_ss5.sh`: real PASS, with a real falsifiability control (HRW re-homes 4 experts vs the modulo-N control's 8 — the cert can distinguish minimal-disruption from a clone, and would RED if HRW regressed). SS-3/SS-5 certs explicitly print "[note] … a true multi-node LIVE … is a deferred `[live]` row." NOCENTRAL is real, not "central deterministically delegated" — `placement.c` greps clean of vote/leader/broadcast and reuses the HRW primitive. interoception slice-1 wires REAL sources (`reflex_threat_experience`, `swim_rtt_ms`, mind surprise, `degrade_level` — `interocept.c:8-12`) and DMN is the one real consumer (`dmn.c:89`). self_access.c has **zero** write/exec/fopen paths — the READ-ONLY claim holds at the artifact level. The KILL-CHURN cure (`2dbacd66`) is a textbook root-cause fix (stale wait-timer firing on a recycled TCB → TS_WAIT guard) with a matched control (crashed run 17 unfixed). This is world-class engineering rigor.

## NAMING-INFLATION LEDGER (name → actual thing)
- **"the brain grows with the fleet" / F1 dynamic size** → `clamp(N,1,16)` that sizes no model (`degrade.c:148`); every model is `_Static_assert`-fixed compile-time. (Doc admits: DECORATIVE.)
- **"one mind" / 心をひとつに** → FedAvg of a fixed-shape 21,568-float `rw[]` vector (`gossip_learn.c`), lossy on divergent facts, requires identical shape fleet-wide.
- **"the conversational mind"** → R3 is a **single-token key→value classifier** (8 keys × 64 answers, `r3_incontext.c:87-88`), no grammar/generation. The byte student babbles multi-byte but is 4 layers / d=128 / 256-byte vocab (`student.h:56-62`).
- **"SS-5 deterministic expert placement"** → a pure HRW hash MAP; no expert ever executes remotely (SS-6 deferred).
- **"N-2 emergent supernodes"** → a `region_supernode()` selector returning a node id; no packet is ever forwarded by a supernode (N-2c deferred).
- **"T-fix-a teacher gossip"** → one capability bit + a selector; no lesson is ever transported or distilled (T-fix-b deferred).
- **"§7 G38.0"** → a behavior-preserving function extraction; the distributed-gating learning (gacc) is on hold.
- **"10,000 nodes"** → DNODE_MAX=64, wire-capped at 254, proven live at N≈3-4. (README honestly calls it 看板/signboard.)

## WHAT TO DO NEXT (prefer PROVE-LIVE / SHRINK over add-and-rename)
1. **STOP the selector march; pick ONE of {SS-6, T-1 bridge, federation R0} and drive it to a real `[live]` N≥3 PASS this next session.** You now have SS-5 (placement map) + N-2b (capability gossip) + SS-3 (cohort merge) + T-fix-a (teacher selection) — four foundations for transports that still don't move a single byte across the wire. Cash exactly one in. "Keep marching the feature backlog" is **the wrong call** right now: it manufactures green deferred-`[live]` rows faster than it manufactures live behavior. The next win must be a process dying and the swarm surviving with the *new* capability, not another pure function.
2. **Make implementer≠auditor STRUCTURAL, not prose.** Have the auditor subagent commit its verdict as a real second commit (or a signed cert artifact) with a distinct trailer, so the separation leaves a trace an external reviewer can check. Right now the project's #1 identity claim is invisible in its own git history. This is the 2026-06-14 critique's unhealed wound.
3. **DELETE the BACKLOG duplication and either DELETE `cap_experts_of` or wire it (SS-4).** A clamp that the doc calls DECORATIVE has sat unused since wave-19; either make it size the router (SS-4, the actual F1) or delete it so no future reader mistakes it for the growth mechanism. Same spirit: dedupe BACKLOG.md before it becomes audit-v9.

## WHAT'S GENUINELY EXCELLENT (kept short)
The falsifiable certs with real controls (SS-5's HRW-vs-modulo-N, KILL-CHURN's matched 17-run repro), the salty-bug-class FP rigor carried forward (`-ffp-contract=off`, `-Werror=vla`, `[no-vla]` greps everywhere), the `[live]`/`[in-proc]` mechanical convention, the willingness to revert two false "ROOT FIX" attempts and keep the row OPEN until a real cure (`6a038d27`, `ae4aa7f8`), and the vision docs that label their own features DECORATIVE/ABSENT with file:line. The honesty *ethos* is real and rare. The failure is that the honesty is applied rigorously to the **code** and only theatrically to the **process** — and this session spent its energy on the safe, green, deferred-half of six features instead of the dangerous live capstone the project keeps naming as priority.
