# Gap Ledger — the single living list of OPEN gaps (canonical)

> One table. The gaps that are **open RIGHT NOW on master**. Proof of progress is
> **rows DECREASING**, not documents increasing.
>
> **Rule of this file (the antidote to audit-sprawl):**
> 1. This table only **shrinks**. New findings **replace** closed rows; they do not lengthen it.
> 2. When a gap ships AND is CI-enforced, **delete its row** (move its epitaph to "Closed" below, one line).
> 3. **Do NOT spawn `philosophy-gap-audit-9`.** v1..v8 are the frozen historical trail
>    (see `README.md` and the banner atop each). The canonical open list is *here*.
> 4. Keep it short. If this file ever rivals the learning code again, it has become the disease it tracks.
>
> Reconciled across `philosophy-gap-audit-{,-2..-8}.md` + `../review-2026-06-three-brains.md`.
> Verified on master `803a465`. Severity: 🔴 contradicts the core · 🟡 weakens it.

---

## OPEN (this is the whole list)

| id | one-line | sev | evidence it is still open (file:line on master) | "closed" means |
|---|---|---|---|---|
| **AUDIT-SPRAWL** (review #5) | The self-audit became a second product (v1..v8 ≈ 2431 lines, rivalling the learning code); gaps were *versioned*, not *closed*. | 🟡 | `philosophy-gap-audit{,-2..-8}.md` (8 files) | This ledger is the sole open list; rows only shrink; no v9 is ever spawned. Closes by sustained discipline. |

**Open rows: 1.** Every other G-number is closed (below).

---

## Closed (epitaphs only — one line each; do not re-litigate)

CI-enforced and shipped, therefore removed from the open table:

- **G20** rally/two-axis gate (flee→approach) — CI `[moe-protect] PASS` `ci.yml:68`, `27_protect`.
- **G28** grounded protect (guarded object is first-class; actuator drives threat→0; survives owner kill) — CI `[protect-ground]/[protect-loop]` `ci.yml:75-76`, live `protect-loop-live` `ci.yml:202`.
- **G35** plural protect (many points defended in parallel; survives owner kill) — CI `[plural-protect]` `ci.yml:79`, live `plural-protect-live` `ci.yml:232`, `28_plural_protect`.
- **G22** collective > individual (disjoint shards, no-central gossip avg, survives kill+rejoin) — CI `[g22-gossip-learn]` `ci.yml:86`, live `collective-learn-live` `ci.yml:263`, `32_collective_learn`.
- **G23** node ceiling > 32 — `DNODE_MAX` 32→64; `GL_MAXNODES`, `KDDS_TOPIC_MAX` (5×), `KDDS_HANDLE_MAX` (10×) now derive from `DNODE_MAX` (one source of truth). Per-node static tables + pmesh BEACON scale automatically; single-char node-topic encoders stay unique to id≈79. `[g23-ceiling]` exercises a 40-model `gl_merge` AND a 40-entry live `dnode_table` membership fold (no 32-cap). CI `[g23-ceiling] PASS` `ci.yml`. **Closed wave-19.** FOLLOW-UP: past id 254 needs a 16-bit `node_id` (wire-protocol change; do NOT bump `DNODE_MAX` alone). Test form: in-process (a >32 live mesh of 33–40 `./p-kernel`+`relay` is the stronger form but too heavy for CI here; the in-process test drives the REAL merge+membership code, not a stub).
- **G33** reflex threat level is driven by the observed danger quantity, not a wall-clock timer — `reflex_threat_level()` returns `reflex_threat_for(danger_active, …)`: a SAFE (class-0) observation clears the latch promptly (no clock advance), while HOLD=5000 is demoted to SHIELD hysteresis and `REFLEX_THREAT_CAP_MS=30000` is a stuck-latch backstop only. `[g33-controlled]`: danger persisting past HOLD keeps level=40 (the old timer would read 0); SAFE drops it to 0; cap clears a silent latch. Audit confirmed case (C) drives the production formula, no coupler (G38/protect) regressed. CI `[g33-controlled] PASS`. **Closed wave-20.**
- **G13** cross-region inference is arrival-driven, not window-gated — the coordinator finalizes the instant every expected live region member has contributed (shared predicate `quorum_core(exp,got,alive)`, used by both the live `cagg_step` and the test); the fixed window (`DKVA_RSUM_WIN_MS`, comment-only; iter cap `DKVA_RSUM_WIN_ITERS`) is a straggler-only safety backstop. Audit found the old fixed-200ms block was ALREADY removed by a prior wave (the ledger refs were stale); the genuine residual was a missing distinguishing test. `[g13-arrival]`: N=8 aggregations finalize in 1 step vs 160 for a fixed window; cap fires only for a never-arriving member. CI `[g13-arrival] PASS`. **Closed wave-20.**
- **G24** durable memory / ARK FS (content-addressed, versioned, crash-safe, power-cut survival on real HW) — `23_durable`, `ark-crash-fuzzer` `ci.yml:358`, `arkfs-audit.md`.
- **G27 / G32** live-path CI gates (self-test green is not promoted; live N≥3 + kill is the gate) — the live jobs above are the institution.
- **G29** + the earlier gaps **G1–G19, G21, G25, G26, G30, G31, G36, G37** — closed across waves 1–16; full provenance in `philosophy-gap-audit-{,-2..-8}.md`.
- **R3** the thinking is no longer a toy — the SAME dtr kernels (anti-fork: shared `dt_linear`/`dt_softmax`/width-parameterized LayerNorm) learn an in-context associative-recall task whose label is resampled every episode. Independently audited on a clean rebuild (gradcheck re-run at stride 1/eps 5e-4: 21516/21516 params agree, zero kink-exclusions): learned **100%** held-out vs handif 35.2% (= chance + bounded value-copy edge) vs frozen 24.7% → margin **+64.8 pts**. CI `[r3-incontext-gradcheck/frozen/handif/learned]`, 28/28 self-tests, all 4 builds. **Closed wave-19** (capacity certificate for the substrate; NOT a live-sensor swap — the conversational mind builds on top). Doc `r3-nontrivial-thought.md`.
- **LM-1 / DMN sleep-consolidation** (north-star *first slice*, not a philosophy gap) — the mind learns a STREAM of region-shift tasks WITHOUT catastrophic forgetting: replay a bounded engram ring (`B_RING=24` ≪ 192/task = 12.5%, printed) → distill via the **G22 no-central `gl_merge`** (anti-fork: trains through `dtr_train_batch`, stores via `pfs_dag_save/read`). Disease is real and measured (task-0 91.7%→**33.3%** no-replay), cure real (replay retains **80.0%**, **+46.7** over no-replay, newest task **95.0%** = plasticity kept). Order-independent merge (no central consolidator), engrams reconsolidate from p-fs after kill+rejoin (**78.3%**), replay path grad-checks (0.002, on a held-out-measured cert). Independently audited on a clean rebuild (verdict PASS; eval confirmed on held-out, not the replayed engrams). CI `[dmn-forgetting/consolidated/distributed/survive/gradcheck]`, 36/36 self-tests, all 4 builds. **Closed wave-21** (extends the existing `dmn.c` organ; honest claim is RETENTION not "zero forgetting"; NOT real language/tokenizer yet). Doc `living-mind.md`.
- **LM-2 / the Self layer** (north-star *second slice*, the least-built worldview layer, not a philosophy gap) — a **distributed autobiographical self**: a per-node, hash-chained narrative lineage (`self/lin` = `LM_SELF_ENTRY` versions; content-level walk via `pfs_id_compute`, **`pfs_dag.c` untouched**; anti-fork: no new hash) that (1) **survives the node's death** and is continued by a successor (reconstructed hash-for-hash from the persisted content-addressed store after a RAM drop; successor `seq==N+1` links to the restored head), (2) is **tamper-evident & fail-closed** (a one-byte flip moves the content-id; a forged splice is rejected; a clean chain still verifies), (3) reconstructs **ownerless** from a peer subset with the origin store emptied. Reuses `pfs_id_compute`/`pfs_get`/`pfs_dag_save`/`pfs_durable_restore` + `drpc_my_node` + `GENOME_WEIGHTS_REF` + `LM_ENGRAM`. Honest bound: **tamper-EVIDENT, not tamper-proof** — no signature primitive exists, a from-genesis fake is still possible; signatures deferred. Independently audited on a clean rebuild (verdict PASS; death real, ownerless genuine, walker fail-closed). CI `[self-continuity/tamperevident/ownerless]`, 52/52 self-tests (0 FAIL), all 4 builds. **Closed wave-22** (the G24/G22 delta is identity semantics — origin-stamped ordered chain — neither has; NOT a learned/semantic self-model yet). Doc `living-mind.md` III.
- **LM-3 / salience-weighted replay** (north-star *third slice* = the DMN's "imagination"; not a philosophy gap) — during DMN sleep the danger class (the one the **reflex actually guarded**) is rehearsed MORE: reallocate the **FIXED** `B_RING` engram slots by **EARNED** salience (`classw` derived from `reflex_threat_experience`/`guard_class_exp`, G38 arrow-2; mirrored `gl_build_weighted` clamp, **no fork, no bigger ring** — total 72 engrams unchanged). Earned not hand-set: the cert drives real `reflex_on_inference` firings (save/restoring live guard counters via new `reflex_guard_exp_save/restore`) then reads the count back. `[salience-earned]` (exp 8>0, `classw[danger]=3`, 24→42 danger engrams at fixed total), `[salience-retains]` (dgain **+16.7** ≥ +5, dgain ≥ sloss **15.0** = net survival-favoring, macro not collapsed, danger headroom acc_uni 60.0 ≤ 90 — a real budget-conserving **tradeoff**, measured on held-out), `[salience-noregress]` (no experience → `classw` all 1 → byte-identical to LM-1 uniform; all `[dmn-*]` stay green). Honest bound: a **reallocation tradeoff, NOT free retention**; salience EARNED not hand-set; prioritized experience replay, NOT generative imagination. Independently audited on a clean rebuild (verdict PASS; budget fixed, salience earned, tradeoff honest). CI `[salience-earned/retains/noregress]`, 55/55 self-tests (0 FAIL), all 4 builds. **Closed wave-23.** Follow-up (audit note, non-blocking): `dmn test` reflex hermeticity — cert restores only `guard_class_exp`+`enabled`, not all reflex stats fields (harmless: no downstream CI reader). Doc `living-mind.md` IV.
- **LM-4 / the fast→slow handoff** (north-star *fourth slice* = in-context knowledge becomes weights; not a philosophy gap) — a fact taught **ONLY in-context** (a fixed dictionary `D*`, read by R3's FAST layer with **frozen weights**) is **transferred into the weights** `rw[]` by a DMN-style self-distillation consolidation round, so the mind answers `D*` **with the prompt REMOVED** above chance, while before sleep the same frozen weights are at chance. Slow layer = **R3's own `r_backward`** (the `[r3-incontext-gradcheck]`-certified gradients), **NOT** `dtr_train_batch`/`gl_merge` (wrong network — the V.0 framing correction; those names appear only in a do-NOT-use comment). Teacher = frozen R3 on the SUPPORT prompt (self-distillation); student trains MASKED toward `ŷ_k`; eval graded vs the **oracle** `D*` on a **disjoint** held-out seed; `rw[]` snapshot/restored between the real- and scrambled-teacher runs. `[handoff-fast-only]` (acc_support **99.8** ≥50, acc_masked_pre **18.8** ≤33 with the SAME frozen weights → `D*` not baked in, gap **81.0** ≥25), `[handoff-consolidated]` (acc_masked_post **100.0** ≥50, gain **+81.3** ≥20 — the handoff happened), `[handoff-grounded]` (teacher_agree **100.0**, train_seed≠eval_seed, acc_masked_scrambled **0.0** ≤33 → a teacher that never read `D*` gives **zero** transfer, so the gain traces to the genuine in-context reading). Honest bounds: toy R3 synthetic vocab (NOT natural language), supervised **self**-distillation (NOT label discovery), held-out **arrangement** not held-out facts, ONE `D*`/ONE sleep (NOT lifelong), in-process CI cert (NOT the live `dmn_idle_work` hook). **No bar lowered** (every margin wide). Independently audited on a clean `make clean` rebuild on the auditor's own binary (verdict PASS; the gate `if` read line-by-line = spec V.4 verbatim, anti-fork grep clean, no leakage, scrambled control isolated, N=400 precondition robust). CI verb `handoff test` + `[handoff-fast-only/consolidated/grounded]` greps (`ci.yml:57,154-156`); 18/18 tags PASS in-sequence (r3+handoff+dmn+salience+self), no downstream regression. **Closed wave-24.** Doc `living-mind.md` V.
- **RING3-B / kernel survives a ring3 core crash** (Evolution-layer foundation slice #1, mk_pino's ring3/EL0 directive; not a philosophy gap) — the bare-metal x86 kernel **no longer dies with its tenant**: exception stubs pass the CPU-saved CS/EIP; `exception_handler` branches — a ring0 fault still prints + halts (honest), a **ring3** fault prints one diagnostic, bumps `ring3_faults_reaped`, records `last_fault_from_ring`, and reaps via the SYS_EXIT unwind (`user_fault_reap`→`user_proc_unwind`→`tk_ext_tsk`, **never iret back into the dead context**). `SYS_INFER` 0x210 repointed `mlp_forward`→`moe_infer`; the class crosses ring3→ring0 via `sys_exit(class)` (a fault-reap records −86, so a crash cannot impersonate a clean infer). Gate `ring3 test` = 6 clauses of `ring3-core.md` II.3 **+ a 7th stronger** (`crash_exit==−86`): live ring0 `moe_infer` oracle (no constant), `reaped Δ==1` exact, `from_ring==3` exact, handler-returned, post-crash sentinel-counter strictly advanced, restart reproduces the oracle. Falsification verified by the **auditor** (defanged crash binary → `FAIL g2-reaped!=1`). Honest bound (gate text + code comments say so): **the moe math still runs ring0 behind the syscall — only survival+restart is claimed; Wave C links the core body into the ring3 ELF.** Independently audited on a clean rebuild + cold QEMU boots (8/8 PASS, anti-fork clean, EIP in the diagnostic objdump-matched to the real faulting instruction). CI: new `ring3-survival` QEMU-boot job greps `[ring3-survival] PASS`; build-only job intact. **Closed wave-25.** Follow-up (audit, pre-existing debt NOT this slice): `dproc_kill_by_name` (`arch/common/dproc.c:221`) terminates with `tk_ter_tsk`/`tk_del_tsk` but no ssy cleanup / paging unregister (leaks the victim's page tables per kill) — give it a `user_proc_unwind`-style teardown. Doc `ring3-core.md` II.
- **LM-5 / 随時, the living consolidation loop** (north-star *fifth slice* = a STREAM of facts, MANY sleeps, the REAL idle hook; not a philosophy gap) — F=4 disjoint fact-sets arrive over time (`r3_fact_learn`: the FROZEN fast layer reads each SUPPORT prompt at arrival → fact-engrams `(k,ŷ_k)`, bounded FIFO queue `R3_FQ_MAX`), and bounded sleep rounds (`r3_consolidate_idle_round`, ≤`R3_IDLE_STEPS`=256 steps — **the exact symbol `dmn_idle_work` now calls**, G33 held: the cert drives the production formula, the naive control is only the spec's `with_replay=0` knob) consolidate them into `rw[]` via `r_backward`. **Disease real:** naive pending-only sequential consolidation collapses fact-1 100→**35.0** (drop **65.0**; every pre ≤33, max 12.0). **Cure real:** LM-1's interleave discipline transplanted to R3 — replay end **100.0 on all four facts**, cure **+65.0**, newest 100.0 (plasticity). **Grounded:** teacher_agree 100.0×4, seeds disjoint, scrambled-arrival stream → **0.0×4** (zero transfer). **Livehook structural:** steps==bound every round, rounds==bound every fact, exactly one FIFO eviction (evicted fact decays 100→53.5, printed NOT gated — honestly an active re-bind by the 5th fact, not passive decay). **DMN wired onto the fleet:** `dmn_init`+`dmn_task`(prio 13) added to BOTH linux usermains — the sleep organ was x86-only dead code on every hosted node until this wave; live idle pulses now genuinely consolidate (observed end-to-end), and the prio-13 task does not perturb the deterministic certs (byte-identical numbers with it live). Two self-flagged deviations audited HONEST: SDICT re-derived to satisfy the GATED ≤33 precondition (naive reuse pre-knew fact 4 at 44.5; same table reaches 100 in the cure → collapse is interference, not impossibility) and 5-read-majority arrival engrams (≤ LM-4's effective teacher; scrambled still 0.0). Honest bounds: adversarial fixed-table cert (milder streams interfere less), no conversational producer yet (the cert is the only `r3_fact_learn` caller today), toy vocab. Independently audited on clean rebuilds of BOTH hosted targets — **byte-identical** stream output cross-arch; 49/49 CI greps; no bar lowered. CI verb `handoff stream` + `[stream-interference/consolidated/grounded/livehook]` greps. **Closed wave-26.** Doc `living-mind.md` VI.
- **RING3-C / the mind's math moves into ring3** (Evolution-layer foundation slice #2 — mk_pino's directive now TRUE for the inference path; not a philosophy gap) — the SAME `arch/common/moe.c`+`dtr.c` sources (whole-file dual-compile, `-ffunction-sections`+`--gc-sections`, zero undefined symbols; **14-symbol zero-math shim** `ushim.c`; `drpc_my_node=0xFF` forces the moe local-only early return so net stubs never execute) compute the class **in ring3** (`core_mind.elf`, 13,076 B) on a live weights copy fetched via NEW `SYS_DTR_WEIGHTS_GET 0x213` (bounded 635 floats over the existing `dtr_weights_get`); ring3 inferences stay **visible** to reflex/world via NEW `SYS_MIND_NOTE 0x214` (op/cls validated, no user derefs). **The fake-resistant proof that the math ran in ring3:** (a) `kernel_infer_count` — 3 counted sites (`train_forward` dtr.c:749, `run_embed_seq` :398, `mlp_forward` ai_job.c:135) that the **auditor traced to cover EVERY class-producing syscall route** (SYS_INFER/DTR_SUBMIT/INFER_SLA/AI_*/drpc) — must stay **delta==0** across every ring3 run; (b) **poison test** — perturbing ONLY the user-side copy (`b_cls`, flat 632) flips the ring3 answer (`c_pre==oracle && c_post!=oracle`) while the ring0 oracle stands; (c) nm tripwire in CI (necessary-not-sufficient — proven by the auditor's relay sabotage). Gate `ring3 mind` = M1–M8 all exact `==` (oracle FIRST then counter snapshot; fat-ELF crash re-proves the Wave B reap clauses; restart==oracle). **Auditor ran two sabotages on its own rebuild:** SYS_INFER relay → `FAIL m2-kdelta!=0` (nm stayed green, M2 caught it — III.4's predicted linked-but-bypassed case); poison removed → `FAIL m3-poison-not-felt`; restore bit-identical (md5). 2 cold boots green; `ring3 test` clauses verbatim (helper gained a cmdline param, behavior identical); hosted smoke all tags green (r3/handoff/stream/dmn/self — the counter perturbs nothing). Design-gap fix en route: native-ELF argv frame in `elf_loader.c` (III.1.4 had overstated). Honest bounds (III.5): inference path ONLY — dtr training/lm/dmn/gl still ring0; weights SNAPSHOT not live-shared; one address space; **x87 FPU save/restore absent** (gate safe only because sequenced+quiesced — MUST precede concurrent ring3 minds); flat space = pointer-taking syscalls unvalidated against kernel ranges (pre-existing pattern, future isolation wave). Build trap for posterity: `make disk` is a NO-OP if `disk.img` is newer — `rm disk.img` before falsifying or you boot stale ELFs. CI: one boot, two verbs, both PASS greps + nm step. **Closed wave-27.** Doc `ring3-core.md` III.
- **DEBT-1+2 / dproc kill teardown + x87 FPU context save** (the two audit-found kernel debts from RING3-B/C; prerequisites for concurrent ring3 minds) — (1) `user_proc_teardown(tid)` factored from `user_proc_unwind` (ssy cleanup, stdin unblock, **CR3 switch BEFORE page-table destroy**, fpu reset; double-destroy guarded), callable from the KILLER's context — `dproc_kill_by_name` no longer leaks the victim's page tables. `[dproc-teardown]`: pool returns to baseline over 4 kill/revive cycles, **disease captured pre-fix** (3→6 climbing). (2) **Eager fxsave/fxrstor** at the single `TCB_SSP` save/restore point (`knl_dispatch`; force-dispatch shares the loop), 512B `aligned(16)` side table indexed by tskid, **zero TCB/offset changes**. `[fpu-ctx]`: deterministic cross-task x87 corruption (errsA=16, bit-identical across boots — task A reads task B's 1000.25f) cured to 0; concurrent ring3 mind correct. Independently audited (clean rebuild, both diseases re-run by the auditor, falsifiable greps, alignment/liveness verified). Honest bound: phase2 disturber pressure weak under TCG (loops=1, printed; phase1 is the deterministic proof); AVX/XSAVE not covered (nothing in the -m32 port emits it). **Closed wave-28.** RESOLVES the RING3-B follow-up (dproc teardown).
- **LM-6 / the mouth** (north-star *sixth slice* = the first REAL conversational producer; not a philosophy gap) — the owner teaches the LIVE mind from the shell: `mind teach <k> <v>` enqueues a singleton fact via the production `r3_fact_learn` (frozen-FAST reading at arrival; re-teach REFUSED — belief revision is its own future slice); the **real DMN idle pulses** consolidate it (`[dmn] sleep: distilled in-context facts -> rw[]` printed exactly **10×** per teach = the 1000ms heartbeat; `dmn_r3_round_count++` at ONE site, the dmn.c hook success branch — `mind_cmd` never calls the round, grep-confirmed); `mind ask <k>` answers MASKED from the weights. Lazy deterministic `s_pretrain` (boot `rw[]` is zeroed statics); `r3_round_busy` quiesce (the shell preempts the prio-13 DMN mid-round — the VII.4 hazard, now built). `[teach-arrival]` (three-way PASS/REDUNDANT/FAIL; teacher_agree **100**, pre_share **0.0** ≤33), `[teach-live]` (drain **15.1s** native / 47.2s qemu ≤120; rounds delta **10**==bound; the production sleep print CI-grepped), `[teach-consolidated]` (post share **100.0** ≥75, N=40). Byte-identical across arches; 25-tag full chain no-regress; LM-5 numbers unchanged; 53/53 CI greps. Two implementer agents were session-limit-truncated mid-verification — commander checkpointed; a SEPARATE agent verified on its own rebuild; gates commander-read. Honest bounds: synthetic vocab (a teach is `k∈0..7 → v∈0..3`, NOT language); owner-teaches (no autonomous extraction); within-node (cross-node = "the shared mind", future); max 4 live facts (R3_FQ_MAX, FIFO + printed forgetting). **Closed wave-29.** Doc `living-mind.md` VII. The galaxy (observation window) + ark-profile (人類の記憶) slices ride this mouth.
- **GALAXY-1 / the observation window — 銀河** (the ワクワク/experience layer; not a philosophy gap) — every hosted node serves its OWN galaxy at **127.0.0.1:7800+(id)**: `GET /` (embedded canvas page, zero external URLs), `/galaxy.json` (me + peers from `dnode_table`/`world_peer_*` + regions + real DMN state), `/events` (SSE from a bounded overwrite-never-block ring; S1-S6 hooks, one `galaxy_emit` line each, first instruction `if(!galaxy_on)return`), lazy `/self.json`, `POST /teach`,`/ask` **through the LM-6 production mouth** (`m_gate` maxsem-1 serializes shell-vs-web; galaxy.c greps clean of `r3_fact_learn`/`s_round`). **No animation theater:** the teach drain is genuinely DMN-driven (auditor: 10 SSE consolidate events == 10 production sleep prints, `dmn_r3_rounds` 0→10); drpc_in events correspond to real remote inferences. **The observer never destabilizes the organism:** LM-6 numbers **byte-identical** galaxy-ON vs `PKERNEL_GALAXY=0`; full chain 52/52 with the galaxy live; adversarial pass survived (path traversal→404, >256B body→413, slow-loris on all 4 client slots→fresh request still 62ms; bind hard-coded loopback, no INADDR_ANY path, 6 endpoints only, no file serving). **The audit FAILED the first pass on a REAL regression** — `galaxy_emit` in shared TUs broke both bare-metal links (galaxy.c is hosted-only) → fixed with a `_TK_HOSTED_LIBC_`-guarded no-op inline in galaxy.h; all targets re-verified. Honest bounds: v1 hosted-only (netstack TCP is client-only — `netstack-tcp-server` is the named bare-metal slice); localhost-only (remote fleet view = future); browser pixels not certified (data plane is the gate); /self.json serves the lineage HEAD (per-seq walk = follow-up). Cert `samples/38_galaxy/galaxy_cert.sh`, CI `[galaxy-serve/events/teach]`. **Closed wave-30.** Doc `galaxy.md`.
- **KILL-TIMER-RACE (open follow-up, audit-found wave-28)** 🟡 — intermittent ring0 #PF in `knl_wait_release_tmout` (+0x11 ≈ `QueRemove(&tcb->tskque)`, `kernel/common/wait.c:111`) during heal-restart of a killed daemon: a **stale timer event fires on a ter/del'd TCB**. PRE-EXISTING (identical signature reproduced on pre-wave master, 1/4 stress boots) — NOT introduced by wave-28, but `dproc test`'s 5 kills/boot raises ring3-CI flake exposure to ~15-25%/boot. Fix shape: `tk_ter_tsk`/teardown must cancel/flush the victim's timer-queue entries before `tk_del_tsk`. "Closed" = the signature is gone over a ≥20-boot kill-burst stress AND the mechanism is named in code.
- **3BRAIN / G38** (review #1·#3) the three brains are one — `moe_infer` runs **one** dtr forward whose argmax is the routed, returned, and guarded class; gate if-ladder + `(void)hum/press/light` and `mlp_forward` are gone from the live path (`drpc` infer uses `dtr_classify`). CI `[onebrain-unified]/[onebrain-channels]/[onebrain-nomlp]/[onebrain-accuracy]` `ci.yml:75-78`, RETURNED accuracy 33%→83% via G22. **Closed wave-18-A.** Residual (honest, out of live path): `spec.c` band partitioner predicts band≠class by design; `mlp_forward` remains only in demo paths (edf/pipeline/fedlearn/ai_job task).

---

## Closure record — wave-18-A (three-brain unification) ✅

The acceptance test below was the gate for deleting the 3BRAIN row. All REAL tokens went green
and every FAKE tell was confirmed absent on a clean local rebuild (commander-verified, not the
agent's binary). Kept as the epitaph's proof; **do not re-litigate**.

REAL — all green:

- **One brain, one answer.** `[onebrain-unified] PASS` — for 4 inputs, `returned==routed==guarded==dtr argmax`,
  all from the single `dtr_decide()` forward at `moe.c:439`.
- **All four channels matter.** `[onebrain-channels] PASS` — per-channel |Δprob|×1e-3 = temp 1, hum 9,
  press 21, light 33 (all nonzero). The `(void)hum/press/light` discard is gone.
- **Live path follows the learned brain.** `[onebrain-nomlp] PASS` — on an input where the handwritten
  MLP says 0 and the learned dtr says 1, `moe_infer` returns 1.
- **Learning lifts the RETURNED accuracy.** `[onebrain-accuracy] PASS` — RETURNED-class accuracy
  UNLEARNED 33% → G22-LEARNED 83% (3 nodes, leave-one-class-out, no-central gossip).

FAKE tells — all absent in the live path:

- `grep -nE 'if \(temp < 20\)' arch/common/moe.c` → no hit.
- `mlp_forward` does **not** feed `result_class` in `moe.c` (only call is inside the `[onebrain-nomlp]`
  test); `drpc` infer dispatch uses `dtr_classify`. The `temp HIGH` constants survive only in demo paths.
- `result_class`, gate, and reflex class are all assigned from the one `learned_class`.
- `(void)hum` appears only in comments describing the *old* code.
