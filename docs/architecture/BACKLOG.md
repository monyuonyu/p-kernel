# BACKLOG — the master ordered TODO (mk_pino: "順次 全部 やりたい")

The single source of truth for everything designed/decided but not yet done. Marched through
in dependency order, each item as a background **implement → independent audit → commander
integrate** wave (the development-method constitution). Disjoint-file items run in parallel;
same-file items serialize. Update this file as items land.

## ✅ DONE (shipped, on trunk, on the phones at 0.9.2)
Chat-as-student (⑥), living-body inspector (organs→real vitals, 3D rings, amoeba, immersion),
4-panel intro, legible biometrics, baby-births-on-phone, **SS-1** (adaptive-K = heavy→wider
firing), **SS-2** (tier scaffolding S/M/L), in-kernel SmolLM2 teacher engine, the build-link
weak-stub fix, flash-wear throttle. Ed25519 signing, persistence, living-mind LM-1..11,
inference-engine M1 (gguf/quant/forward/tokenizer), galaxy, ark-profile — all already live
(their docs' "not started"/"実装前" Status lines are STALE → fix them, see bottom).

## 🟡 IN FLIGHT (background lanes)
- **② full SMP (★ mk_pino's END GOAL) — GO given 2026-06-20, IN PROGRESS.** Design plan `full-smp-plan.md` DONE. **②.0 SHIPPED + audit MERGEABLE 2026-06-21 (78c5d222):** 2 aarch64 CPUs run a per-CPU dispatcher under ONE Big Kernel Lock, provably concurrent (distinct MPIDRs; NOLOCK falsifier loses ~half updates), shipped uniprocessor kernel BYTE-IDENTICAL (own dispatcher gated behind SMP_SELFTEST; production knl_ctxtsk untouched), mind untouched. Certs [smp-2-tasks-run]/[smp-mutual-exclusion]/[smp-boot-survives] on QEMU -smp. **②.1a SHIPPED + audit MERGEABLE 2026-06-21 (c2bba938):** p-kernel's first IPI — one core writes GICD_SGIR, another takes the SGI through the production IRQ vector (unforgeable sgi_taken proof) and preempts to a higher-prio task; the SGI is the SOLE trigger (-DSMP_NO_IPI falsifier never switches); shipped kernel byte-identical; HONEST: cooperative-at-checkpoint, not true async (= ②.2); RPi3 mailbox deferred. **REMAINING: ②.1b** (idle-CPU wakeup-via-SGI + N=4 + the run_smp1/smp0 harness-snapshot fix) -> **②.2** (per-CPU-ize the ~166 production knl_ctxtsk sites + the [smp-one-mind] crown cert: real mind byte-identical under SMP) -> **②.3** (finer locks) -> hosted-port SMP -> RPi3 [live] (the only place barrier/race teeth bite). FOLLOW-UP: run_smp0.sh rebuilds kernel.elf in the boot dir (snapshot before boot). Per-wave impl->audit.
- (idle — SS-6 ✅ merged AND **SS-6-`[live]` ✅ CASHED (wave-ss6-live): a REAL 4-process cross-node student forward over `./relay`, byte-identical to single-node, 266 experts on peers over the wire**. Next per the harsh-review pivot: keep cashing ONE thing at a time to `[live]`. Candidates: federation R0.1 (resp-scope + coord-crash-live), the SS-6 KV `kv_step`/live-chat wiring (the remaining SS-6 follow-up), or N-2c packet forwarding.)

## ✅ DONE THIS SESSION (on trunk `9e50e4a8`, NOT yet in an APK — next APK = 0.9.3)
> **HONEST FRAMING (2026-06-20 harsh review, finding #2):** most of this session shipped the SAFE half of each feature — a selector / a map / a seam / an `[in-proc]` cert — and DEFERRED the load-bearing distributed/learning half to a `[live]` row. The mind is not materially more capable; the scaffolding is. **The next win must be ONE thing driven to a real `[live]` N≥3 PASS, not another pure function.** (See QUEUE.)
- **KV cache** (`4c58a231`): O(1)/byte incremental gen, byte-identical to recompute cross-arch, 4.66→50x. ← a genuine capability gain (chat is faster). Audit PASS.
- **SS-3 cohort merge** (`7ec1ec54`) · **SS-5 placement** (`9e50e4a8`): same-tier weight-average + HRW expert→node MAP. **[in-proc] only — no expert executes remotely (SS-6); no blob crosses the wire (`[live]` deferred).**
- **N-2 selector** (`1f656c3c`) · **N-2b capability gossip** (`83f20dbd`) · **T-fix-a teacher selector** (`5270bbae`): lowest-id selectors + a gossiped capability bit. **No packet is forwarded by a supernode (N-2c); no lesson is transported/distilled (T-fix-b). SELECTION ONLY.**
- **§7 G38.0** (`9f7a9bc4`): a behaviour-PRESERVING `moe_select_step` extraction. **Moves no digits; the gacc learning is ON HOLD for mk_pino.** Value = a tripwire seam, not a feature.
- **self-access R0** (`d730175e`): READ-ONLY `body` introspection + Q3 lineage (1/invocation, tamper-evident). ← a real, complete capability. Audit PASS.
- **interoception slice-1** (`9e5529a2`): the unified stress **S_n bus** (real sources: reflex-threat/SWIM-RTT/surprise/fault/degrade) → DMN idle-threshold modulation + galaxy me-star real mood. ← real, the one live consumer is `dmn.c`. Audit PASS. Apoptosis (slice-2) deferred.
- **Design plans** (`survival-g38-impl-plan.md` §7 gacc · `thread-t-impl-plan.md` teaching · `federation-r0-plan.md`): cert-first plans that each found a real bug — **but in code that is NOT built (motion → a doc, per finding #3). Not defended behaviour.**

## ▶ ORDERED QUEUE (the marathon)

### Thread N — Network = the "Skype-like" P2P (mk_pino's passion; verdict SOUND)
- **N-0 decentralized node-id** (HARD PREREQUISITE — two phones default to id=1 → SWIM ignores each as "self"). Key-derived/random unique id. [touches the boot/id path]
- **N-1 LAN-direct** `net_lan.c` (3rd transport backend behind PKERNEL_LAN=1; net_unix.c template, 127.0.0.1→255.255.255.255:7351 + learned-peer table; SWIM's broadcast beacon becomes real LAN UDP). → **two same-WiFi phones auto-mesh, NO relay.** Host-first (2 namespaces/veth; SO_REUSEPORT caveat), then Android (CMake TU + parity). [net layer]
- **N-2 emergent supernodes** (relay forwarding logic lifted into capable nodes, NOCENTRAL deterministic-from-SWIM). **N-3 NAT hole-punching** (supernode-assisted; cone-NAT only, symmetric stays relayed). **N-4 bootstrap/seed** (PKERNEL_SEED list; relay = one optional seed). Doc: p2p-overlay.md.
  - **N-2 slice 1 DONE** (wave-n2-supernode-select): the deterministic **selection function** `region_supernode()` in `arch/common/region.c` (lowest-id node that is BOTH a region member AND supernode-capable; `0xFF` = degrade to central relay) + a local capability table/setter + `PKERNEL_SUPERNODE=1` self opt-in + host cert `region_supernode_test()` (verb `region test`; 8/8 PASS: lowest-capable-wins, convergence/determinism, survives-death by recompute (no vote), relay-fallback, non-member-capable ignored). Integer-only → cross-arch identical.
  - **N-2 slice 2b DONE** (wave-n2b-capability-gossip): SWIM **capability-bit gossip** — the self-declared capability rides each node's own ALIVE gossip in the `_pad`→`capability` byte of `SWIM_GOSSIP_EVT` (wire byte-identical, `SWIM_VERSION` unchanged; old nodes emit 0 → relay-fallback). Self-authoritative origination, verbatim relay, applied under the existing `(incarnation,state)` last-writer-wins gate so it converges in lock-step with membership and never regresses on a stale rumor. `region_supernode()` math untouched; the table is now fleet-real. Host cert `swim_cap_gossip_self_test()` (verb `nodes cap`; converge/staleness/falsifiable, cross-arch PASS, no swim/region regression). **DEFERRED to later N-2 slices:** supernode packet forwarding (N-2c, relay REL_DATA/REL_BROADCAST relocation), NAT hole-punch (N-3), seed bootstrap (N-4); runtime capability flip (needs incarnation bump) + true multi-process live-mesh `[live]` cert.

### Thread M — the special-structure MIND = 完全体 (special-structure-mind.md)
- ~~**SS-3** same-tier merge cohorts~~ **SHIPPED 2026-06-19 (wave-ss3-merge-cohorts):** student-only `st_merge_cohort` (peer-symmetric, canonical reduction, one-math) + `st_blob_tier_ok` cross-tier refusal (islands) + chunked p-fs transport `gl_student_publish`/`_fetch`; NEVER calls `gl_merge`/touches `rw[]`. Cert `tests/llm/run_ss3.sh` 6/6 (`[ss3-cohort-converge]` merged loss ≤ worse parent + byte-symmetric, `[ss3-cohort-island]`, `[ss3-merge-falsifiable]`, `[baby-merge-isolation]`+`[no-vla]`). HONEST: averaging is the mechanism (converges a SHARED objective; LOSSY for divergent facts — Path-W², out of scope); LIVE chunked publish/fetch over the relay is a DEFERRED `[live]` row (converge claim is in-process).
- ~~**SS-5** deterministic expert placement map~~ **SHIPPED 2026-06-20 (wave-ss5-placement):** NOCENTRAL local function `st_expert_owner(expert_id)` = HRW (rendezvous-hash) winner of (expert-id, alive-member-set), computed locally from the SWIM `dnode_table[]` view — no broadcast/vote/leader/new-gossip; re-derives on membership change (dead node's experts re-home, all others UNCHANGED — minimal disruption). New module `arch/common/placement.c`/`.h` (kernel tier, builds on all four boots); a THIN shim REUSING `lookup_responsible()` (`arch/common/lookup.c`) — stable per-expert sha256 key ⇒ byte-identical cross-ABI, integer+sha256 only (no float). Cert `tests/llm/run_ss5.sh` 8/8 + shell verb `place` (`[place-deterministic]`, `[place-rehome]` w/ falsifiable modulo-N control 8 vs HRW 4, `[place-balance]`, `[nocentral]`/`[no-float]`/`[no-vla]`). HONEST: placement MAP only — remote-expert EXECUTION is **SS-6 (DEFERRED)**; single-node = all experts → the one node (the map is SS-6's foundation); a multi-node LIVE convergence run is a DEFERRED `[live]` row.
- ~~**SS-6** cross-node expert firing with local fallback~~ **SHIPPED 2026-06-20 (wave-ss6-cross-node):** the F4 capstone — "複数ノードをまたぐ一回の forward". When the SS-1 adaptive router WIDENS K beyond K_min on a hard token, the EXTRA experts (chosen-slot j ≥ K_min) that SS-5 placement puts on a PEER are computed REMOTELY (the peer runs that expert's SwiGLU on the `[D]` f_in vector, ~D floats on the wire) and their `[D]` outputs are summed into `moe[]` in a FIXED canonical reduction order (ascending slot j == the single-node order — CRITIQUE GATE #3) ⇒ a remote forward equals the single-node forward BYTE-FOR-BYTE. Both branches call the SAME `st_expert_forward_ref` (one math). The local K_min experts ALWAYS run locally; a remote timeout/absent peer RECOMPUTES that expert locally (lose the WIDTH, not correctness — honest degraded). Transport is the caller-installed `st_set_remote_expert(fn, gate, ctx)` hook (kernel wires DRPC; cert wires a stub) — no new transport invented. New API in `student.c`/`.h`; the MoE loop materializes per-expert `[D]` outputs into a file-static `eo_all[KMAX][DMAX]` (no-VLA, no stack-overflow) then folds canonically. Crown: NEVER touches `rw[]`/`gl_merge`; KV byte-identity intact (kv_step untouched, run_kv still byte-identical); single-node path byte-unchanged when the hook is clear. Cert `tests/llm/run_ss6.sh` 7/7 (`[remote-expert-equiv]` M 506 / L 940 experts remote, byte-identical logit hash; `[remote-expert-fallback]` timeout → local recompute, still byte-identical; `[remote-falsifiable]` 1e-6 perturbation FAILS; `[canonical-order]`/`[no-vla]` greps clean). S tier proven INERT (E=2==K_min, no widening, no fan-out). HONEST: in-process cert drives the REAL canonical-sum + fallback path with a STUB peer; a true multi-process forward over the relay is a DEFERRED `[live]` row; the hook is wired into `st_forward` (the canonical forward) — wiring it into the KV incremental `kv_step` generation path is a documented follow-up.
- ~~**SS-6 `[live]`** the multi-process cross-node forward~~ **SHIPPED 2026-06-20 (wave-ss6-live):** the DEFERRED `[live]` row is CASHED. A REAL relay-backed remote-expert transport (`arch/common/ss6_live.c`/`include/ss6_live.h`, UDP port `SS6L_PORT`=7378) mirrors `drpc.c`'s pending-table + reply-semaphore + hard timeout (3×200ms→fallback); the requester ships the `[D]` `f_in` to the SS-5 `st_expert_owner` (HRW, NOCENTRAL — no vote) over the mesh, the owner runs the EXACT `st_expert_forward_ref` and replies the `[D]` output, and `student.c` sums it in the UNCHANGED canonical ascending-slot order. Installed via `st_set_remote_expert` behind a fail-closed gate (`PKERNEL_REMOTE_EXPERTS=1` AND wide slot j≥K_min AND `!st_expert_is_local` AND `degrade==FULL` AND `region_size()≥2`) so a default single node is byte-unchanged and training stays local. Cert `samples/11_distributed/run_ss6_live.sh`: **4 p-kernel PROCESSES over `./relay`, degrade FULL; node 1's LIVE cross-node forward logit-hash == its single-node oracle hash (`00d6136e6d4676a3`), 266 experts computed on PEERS** (266 REQ + 266 REP `[D]`-vector frames on the relay log, sizes 1114/1118 B). Killed-peer arm `run_ss6_live_fallback.sh`: a peer killed but still ALIVE in placement → 4 experts time out → recomputed LOCALLY → STILL byte-identical, `fallback=4`, no stall (honest degraded). NEVER touches `rw[]`/`gl_merge`; **`run_kv` still 18/18 byte-identical (`kv_step` UNTOUCHED)**; `run_ss6` 7/7 unchanged; ss1/ss2/ss5 + dkva/placement self-tests PASS. Built on all four boots (`ss6_live.c` hosted-only in `COMMON_C_SRCS`; not on bare metal). HONEST: the `[live]` forward reaches `st_forward` ONLY — the LIVE CHAT / KV `kv_step` generation path is NOT wired ("live cross-node forward" ≠ "live chat is distributed"); ran 4-process on ONE host (single-host floor; the authorized SSH 2-machine run not attempted this wave); the cert model is M-tier (L overflows the small shell-task stack — L byte-identity already proven in-process by `run_ss6`).
- **SS-4** function-preserving expert growth (cap_experts_of(N) sizes the router) · **SS-7** bigger baby. [all touch student.c — SERIAL]

### Thread T — conversational TEACHING (education 考え方 → ✅ cert-first PLAN DONE: `thread-t-impl-plan.md`)
- ✅ **cert-first impl PLAN DONE** (design-harden workflow): winner = **text-level** distillation (teacher emits TEXT → byte-native student learns bytes directly; builds on the REAL distill path `distill_proof.c` proves weight-resident); transport = KDDS `CRADLE_TEACH` beacon + p-fs body; teacher-selection = **bit 1 of the N-2b SWIM capability byte** (wire-compat, unanimous). Critique caught a real cert bug (positional held-out split → a naive probe cert can FAIL with teaching ON) → corrected cert separates a trained lesson copy from a never-trained held-out probe (= generalization, not rote). Soft/logit distillation DEFERRED behind a `fmt` byte. Depends on Thread N (mostly done) + the teacher engine (exists).
- ✅ **T-fix-a DONE — NOCENTRAL teacher SELECTION** (`wave-tfix-a-teacher-cap`): mirror of N-2b. SWIM `capability` **bit 1 = teacher-capable** (Option A; wire UNCHANGED — `SWIM_GOSSIP_EVT` 4B / `SWIM_PKT` 24B / `SWIM_VERSION` 1, asserts hold; old nodes emit 0 → supernode=0,teacher=0 safe degrade). `teacher_self()` (swim.c) gates bit 1 on a VERIFIABLE loaded teacher GGUF via a **weak `teacher_gguf_loaded()` hook** (strong override in student_shell.c does a one-time cached `gguf_open`+`lm_load` probe, env `PKERNEL_TEACHER=1` necessary-not-sufficient); weak default 0 covers bare-metal/Android (safe degrade). Bit-packed at both origination sites + applied under the SAME `(incarnation,state)` LWW gate (stale rumor cannot flip it). `region_teacher()`/`teacher_capable[]`/`region_{set,is}_teacher_capable()` (region.c) = lowest-id node BOTH member+teacher-capable, 0xFF if none, survives teacher death by recomputation. Cert `swim_teacher_gossip_self_test()` (`nodes teacher`) PASS converge/selector/determinism/staleness/falsifiable (RED when apply ignores bit 1); `region test` adds `[region-teacher] 5/5`; no regression (`[cap-gossip]`/`[swim-incarn]`/`[region-super] 8/8` green). 4-build clean (linux, linux_x86_64, x86, aarch64), cross-arch cert green under qemu. **HONEST: SELECTION only.**
- **NEXT: T-fix-b/T-1 — the lesson BRIDGE** (deferred; serializes after SS-3 frees student_shell.c): `CRADLE_TEACH_PKT` + p-fs body, `g_lesson_ring` corpus seam, `cradle_poll_and_pull()`, the 2-node `[cradle-teach]` demo cert (3 falsification arms). The in-kernel live teacher harvest (a node `lm_load`s a real GGUF and emits lessons) + a true multi-process live teacher-convergence `[live]` row are deferred too. Touches student_shell.c/gossip_learn.c/dtr.h.

### Thread B — the 🅰 big designs (each its own multi-wave)
- **interoception / 負のエネルギー**: ✅ slice-1 DONE (`9e5529a2`: S_n bus → DMN modulation + galaxy hue). **REMAINING: slice-2 apoptosis** via Path-W² essence handoff (delicate — controlled node death + essence transfer; touches dproc lifecycle + gl_merge). Follow-up: discover per-axis normalization bands from measured curves (§2.4; current maps are honest first-cut linear defaults). (interoception.md)
- **self-access / 自分の体を触る**: ✅ R0 read-only introspection DONE (`d730175e`). **REMAINING: R1** the mind invokes `body` AUTONOMOUSLY (DMN idle hook) → **BLOCKED on user decision Q1** (how free is autonomous read-only) **/ Q2** (driver consent once vs each); then drive own shell/storage/devices → self-write+compile drivers. Q3=YES already (log body-touch to lineage). (project_self_access_embodiment)
- **imaginary-UI**: conversation conjures ephemeral GUIs on demand ("電卓が欲しい"→builds one). (web-os.md)
- **compatibility / 凍結なし進化** (DECIDED 2026-06-14): generational succession + per-version migration chain + signed-OTA. (compatibility.md, project_compat_evolution)
- **federation** 254→10k staircase. ✅ **R0 SHIPPED 2026-06-20 (wave-fed-r0):** the 2-cluster DKVA hierarchy is now **certified, with the `[live]` arm actually running on the real multi-process harness** (the 2026-06-20 review's pivot demand). Load-bearing verify DONE: `rsum/<rid>` IS `KDDS_SCOPE_GLOBAL` while `resp/<n>` is `KDDS_SCOPE_REGION` (`dkva.c:809-818`) → the O(#region) property holds (the plan's grepped-line worry was a false alarm). Slices: (1) `dkva_fed2_self_test()` `[fed-2cluster][in-proc]` — production expect-loop refactored into a pure core `dkva_expect_core` driven by both `dkva_infer` AND the test; falsifier `rc_cnt0==1` (O(#region) not O(#nodes)) + summary==dense fold + NOCENTRAL min-id; (2) `[coord-crash][in-proc]` — b1 dead → rep deterministically b2, no election, expect-set re-forms; (3) `[fed-2cluster][live]` in `run_4node_regions.sh` — **8 OS processes through `./relay`**, diffs `[locality]` counters across one infer + a **flat-control falsifier** (`PKERNEL_RTT_ZONE_PENALTY=0`): zoned far=6/near=3 (summary-only crossing) vs flat far=0/near=9 (direct fan-in to all N) → mechanically distinguishes hierarchical from degenerate. **All arms PASS.** NO DNODE_MAX/wire change (the 256-wall raise = a separate F1 wave). `federation.md` §1/§5 stale text corrected. HONEST: `[live]` ran 4-node/2-zone on ONE host (SSH 2-machine not attempted); 10k-dream gap (256 wall, MTU, array fan-out) stays wide open — R0 is the first tread, not the staircase. Audit PASS (independent reproduction 3/3 + broke-the-hierarchy-twice→RED; row CLOSE) — `docs/audit-trail.md`. **R0.1 OPEN (audit-flagged):** the `[live]` gate measures only node1-the-requester, so a `resp/<n>` REGION→GLOBAL degeneration on the RESPONDER nodes would NOT trip it (statically correct + verified, not dynamically falsified) → add an all-node/coordB-side locality gate; + the `[coord-crash][live]` kill arm (deferred from R0).
- **survival-network「考える器官」**: §7 distributed gating (= regions R3 / mutual-aid) · §8 two-layer oscillation fix · decentralized whole-network situational-awareness map. (survival-network.md) — ✅ §7/G38 cert-first PLAN DONE (`survival-g38-impl-plan.md`) + ✅ **Wave G38.0 SHIPPED** (`9f7a9bc4`, audit PASS): the testable seam `moe_select_step` extracted from `select_expert` (behaviour byte-preserved; `[moe-seam]` cert closes trap A2 so a future learning cert can't be a rubber stamp). **REMAINING: G38.1+ the `gacc` local-gradient learning + §8 stabilizer** — ON HOLD pending mk_pino's review of his core §7 philosophy (the commander reads the gate formula line-by-line before crediting; highest-risk gate = the COUPLED pressure+gacc-sum oscillation proof). §8 map + survival slice-2 still queued.
- **r3-nontrivial-thought**: multi-step non-trivial reasoning. (r3-nontrivial-thought.md)
- **multi-core compute ③ — deterministic parallel matmul** (stepping-stone to ② full SMP; kernel stays uniprocessor). Doc: `multicore-matmul-plan.md`. The crown invariant = BYTE-IDENTICAL to serial for any worker count (one mind, one math; output-row partitioning never reassociates → admissible on the `rw[]` merge path, unlike the deferred GPU).
  - **MC-0 SHIPPED + AUDIT PASS 2026-06-20:** `pk_parallel_rows` primitive (`arch/common/llm/pk_parallel.{c,h}`, HOSTED pthread pool, `(out,NW)` partition w/ ragged-remainder-on-last, condvar-BLOCK idle = no busy-spin). `[par-matmul-equiv]` memcmp==0 + FNV equal for `NW∈{1,2,4,8}`; `[par-matmul-falsifier]` reassociating variant DIFFERS (teeth); `[mc0-idle]` ~0 CPU. NO speedup claimed.
  - **MC-1 SHIPPED + AUDIT PASS 2026-06-20:** size-gated (`pk_parallel_rows_gated`, gate=524288 MACs) on the teacher forward; **2.2–2.67×**; full forward logit hash byte-identical at every NW w/ real GGUF; small student + R3 stay inline (below gate).
  - **MC-2 design plan** (`mc2-baremetal-smp-plan.md`) — bare-metal constrained SMP. HONEST: bare metal has NO big matmul today (LLM tier hosted-only; dt_linear ~48×48 < gate) → MC-2's value is the **②-bringup foundation**, not a speedup.
  - **MC-2.0 SHIPPED + AUDIT MERGEABLE 2026-06-20 (b5bacd89):** wake ONE parked aarch64 secondary via PSCI CPU_ON, run a deterministic tile, the kernel SURVIVES it (boots to shell, dispatcher byte-for-byte untouched). Cert `[mc2-boot-survives]`, 4-way falsifiable; faulting-tile bounded-join watchdog. 5-dim adversarial workflow audit, no blockers.
  - **MC-2.1a SHIPPED + AUDIT MERGEABLE 2026-06-20 (bba18e19):** N secondary cores + a real work-queue + `[mc2-smp-equiv]` BYTE-IDENTITY across {1,2,4} (FNV `0xd4c96f986cbafb17` all three). Audit PROVED parallelism real (cores 2,3 ran distinct ranges), found+fixed 2 bugs (join-on-nhelpers race, double-selftest). Tooth A bites; Tooth B (barriers/SMPEN) honestly deferred to RPi3. `mc2-1-ncore-equiv-plan.md`.
  - **MC-2.1b SHIPPED + AUDIT MERGEABLE 2026-06-20 (0ab9e548):** standalone pk_slice_bm partition unit-check (31 cases, DISJOINT+TOTAL/ORDER/MATCHES-GOLDEN) + falsifier; the golden ref proven independent (not circular). Default build byte-identical. **The ③ multicore arc is COMPLETE + audited end-to-end.**
  - **REMAINING:** **MC-2.2** RPi3 hardware `[live]` (the ONLY place the barrier/SMPEN Tooth B bites) · then **② full SMP** (★mk_pino's end goal: per-CPU run-queues + task migration + cross-CPU `knl_ctxtsk` locking + IPIs, ON TOP of MC-2's bringup/per-CPU/work-queue/lock/barrier foundation) · (later) wire a REAL bare-metal matmul (`dt_linear`) to the seam once SS-7 grows R3 past the gate.

### Thread R — roadmap remainders
- regions R3 width · DNODE_MAX raise · lookup L2/L3 · reflex-deliberation D3 (熟慮の中身) · p-fs P3/P4 · ring3 remainder (dtr-train/lm/dmn/gl into ring3, async 0x240/0x241, x87 FXSAVE, aarch64 EL0) · dproc_kill_by_name teardown debt.

### Thread P — product / hardware
- Play Store public release (UMP/ark Phase D) · aarch64 real hardware (RPi3) + netboot · doc physical-halve + artifact rename (the critique's shrink move; "年輪" growth accepted, so this is light).

### Thread X — EXCAVATED (was designed/decided in memory+md but NOT tracked here; folded in 2026-06-20)
- **WebOS — human computing environment** (`web-os.md`, `project_webos_human_environment`): on TOP of yurikago, a HUMAN-facing Web OS served from the node (responsive: desktop on PC, mobile shell on phone) — the human's window onto the SAME p-fs/shell/mind substrate self-access gives the AI. DISTINCT from imaginary-UI (AI-conjured GUIs). Design-doc-first, multi-wave. **[big, untracked]**
- **LM scale wall — a real conversational model** (`project_next_phase_vision` bucket 2; surveys done: `base-model-survey.md`, `conversation.md`, `moe-distillation-survey.md`): scale the byte student toward a genuinely capable conversational mind (beyond watch-class M). SS-7 (bigger baby) is one lever; the broader strategy is bigger. **[strategic]**
- **device-capacity — 端末性能に応じたサイズ** (`device-capacity.md` DRAFT + `device-capacity-verdict.md` = DEFER): tier/neuron-count by device capability. `capacity(N)`/tiers partly wired (degrade.c); device-tier-aware sizing DEFERRED.
- **GPU** (`gpu-compute.md`: Vulkan matmul backend SHIPPED, integration pre-impl · `gpu-3-wiring.md`: design+critique done, **DEFER**): resource-aware GPU acceleration. Partly done.
- **ark app UX queue** (`feedback_ump_ux_principles`): pop picture-rich 3-4 page intro (the big one) · page-chrome i18n for the other ~30 langs · key-derived unique node id · on-device salty-cert harness · MainActivity auto-path log-drain.
- **r3-model-widening** (`r3-model-widening.md`, design only) — likely == Thread R "regions R3 width".
- principle (not a task): **「いいねのない銀河」** (`project_recognition_layers`) — UX rule: feedback from the SYSTEM yes, human-vs-human comparison no.

> **PRIORITY NOTE (2026-06-14 critique steer):** "more features/LM < foundation + **federation** + honest finitude." The foundation crack (KILL-CHURN #PF) is CURED (wave-56); the live remaining steer is **federation** (254→10k) — hence the federation-R0 design-harden in flight. Keep the dream-tier names + co-located honest labels; keep `[live]`/`[in-proc]` mechanical tags.

## DOC-STATUS FIXES (stale "not started/実装前" lines to correct)
galaxy.md, ark-profile.md, signing.md, persistence.md, living-mind.md, inference-engine.md,
living-body-inspector.md — mark what actually shipped. (housekeeping wave)

## RULES (constitution)
Commander orchestrates only; implementer ≠ auditor ≠ commander; background + yield (never sit
idle on one agent); the audit is the immune system; M-tier/determinism/one-mind/NOCENTRAL
invariants are non-negotiable; one shrinking ledger.
