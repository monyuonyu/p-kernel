# BACKLOG — the master ordered TODO (mk_pino: "順次 全部 やりたい")

The single source of truth for everything designed/decided but not yet done. Marched through
in dependency order, each item as a background **implement → independent audit → commander
integrate** wave (the development-method constitution). Disjoint-file items run in parallel;
same-file items serialize. Update this file as items land.

## ▶▶ 次の一手 — 2026-06-27 整頓 (the live "what's actually left" map; full detail below)
This file is ~95% ✅DONE annotations — read THIS block for the forward path, the threads below for detail.

**① in flight (this session, background impl→audit waves):** webd Slice-A finish (Codex's ui_api
boundary made real) · live-3node grep-race v3 · then **enable branch protection once all-green**
(CI flips informational→blocking — the final CI-hardening step) · the lone galaxy-cert red
(UMP x86_64) investigate-or-quarantine.

**② ★STRATEGIC UNLOCK — the self-hosted ThinkPad runner retired *"deferred pending faster host"*.**
The whole `[live]` backlog that was blocked on host speed is now RUNNABLE on CI. Natural next batch:
cradle-live real-ThinkPad re-confirm · **N-1 `[lan-direct]` cert + same-WiFi 2-machine `[live]`** ·
**SS-3 `[live]` step-3** (3-proc relay blob round-trip) · **federation R0.1 independent audit → F1**
(the 254→10k 256-wall raise).

**③ ★mk_pino's declared END GOALS (big, multi-wave, autonomous-OK):** **② full SMP ②.3** (finer
locks + knl_taskindp per-CPU + task migration) → hosted-port SMP → RPi3 · **SS-7 bigger baby /
LM-scale** (toward a genuinely conversational mind).

**④ ⛔ BLOCKED ON mk_pino's PHILOSOPHY CALL — do NOT implement autonomously (3 threads stuck here):**
- **interoception slice-2 apoptosis** → **B-3: voluntary-only, or may the collective "euthanize" a
  sick node?** (+4 his-call questions). A majority that can kill an individual = the seed of
  ownership, against "a home no one owns". Design default = voluntary-only.
- **self-access R1** (the mind autonomously touches its own `body`) → **Q1** how free is autonomous
  read-only / **Q2** driver-consent once-vs-each.
- **survival §7 gacc + §8** distributed-gating learning → his §7 philosophy review (commander reads
  the coupled pressure+gacc oscillation gate line-by-line before crediting).
- STATUS 2026-06-27: mk_pino chose 棚卸しだけ — surfaced here, decision deferred to a later sit-down.

**⑤ product / needs special env:** **Android-parity CMake lock-step (needs NDK)** — net_relay_tcp.c /
supernode_autopromote.c / compat_ota.c etc. are host-only, NOT yet on phones · **APK 0.9.3** (this
session's trunk features unpackaged) · Play Store Phase D · RPi3 hardware + netboot.

**🧹 housekeeping:** merged local `wave-*`/`slice-*` branches pruned 2026-06-27 (commits are in master).

---

## ✅ DONE (shipped, on trunk, on the phones at 0.9.2)
Chat-as-student (⑥), living-body inspector (organs→real vitals, 3D rings, amoeba, immersion),
4-panel intro, legible biometrics, baby-births-on-phone, **SS-1** (adaptive-K = heavy→wider
firing), **SS-2** (tier scaffolding S/M/L), in-kernel SmolLM2 teacher engine, the build-link
weak-stub fix, flash-wear throttle. Ed25519 signing, persistence, living-mind LM-1..11,
inference-engine M1 (gguf/quant/forward/tokenizer), galaxy, ark-profile — all already live
(their docs' "not started"/"実装前" Status lines are STALE → fix them, see bottom).

## 🟡 IN FLIGHT (background lanes)
- **SMP-N8 SHIPPED + audit MERGEABLE 2026-06-21 (5922fa91):** the SMP machinery scales to 8 physical cores (GICv2 ceiling); -smp 8 wakes 7 secondaries, counter==1600000, default byte-identical. **Runtime core-count AUTODETECT designed** (`device-autodetect-plan.md`, GICD_TYPER): SMP_MAX_CPUS=8 becomes the array ceiling, runtime g_smp_ncpu=min(detected,8) so the SAME binary adapts (RPi3=4, phone=8) — impl queued after this. mk_pino's direction: measure the device + auto-fit (cores now; the mind-sizing = the deferred device-capacity thread).
- **② full SMP (★ mk_pino's END GOAL) — GO given 2026-06-20, IN PROGRESS.** Design plan `full-smp-plan.md` DONE. **②.0 SHIPPED + audit MERGEABLE 2026-06-21 (78c5d222):** 2 aarch64 CPUs run a per-CPU dispatcher under ONE Big Kernel Lock, provably concurrent (distinct MPIDRs; NOLOCK falsifier loses ~half updates), shipped uniprocessor kernel BYTE-IDENTICAL (own dispatcher gated behind SMP_SELFTEST; production knl_ctxtsk untouched), mind untouched. Certs [smp-2-tasks-run]/[smp-mutual-exclusion]/[smp-boot-survives] on QEMU -smp. **②.1a SHIPPED + audit MERGEABLE 2026-06-21 (c2bba938):** p-kernel's first IPI — one core writes GICD_SGIR, another takes the SGI through the production IRQ vector (unforgeable sgi_taken proof) and preempts to a higher-prio task; the SGI is the SOLE trigger (-DSMP_NO_IPI falsifier never switches); shipped kernel byte-identical; HONEST: cooperative-at-checkpoint, not true async (= ②.2); RPi3 mailbox deferred. **②.1b SHIPPED + audit MERGEABLE 2026-06-21 (6b24aa97):** SMP self-test scales to N=4 (counter==4*K, variable NOLOCK loss, distinct per-CPU stacks) + the harness-snapshot fix (the ②.0-audit follow-up; cert harnesses now boot a snapshotted elf so a concurrent rebuild can't corrupt a run); production scheduler untouched, default byte-identical. (idle-wakeup-via-SGI folded into ②.2.) **②.2a SHIPPED + audit PASS + commander-merged 2026-06-21 (9e476954):** the PRODUCTION T-Kernel scheduler is now SMP — ~211 knl_ctxtsk/schedtsk sites -> CUR_* macros (per-CPU under SMP, plain-global otherwise = N=1 BYTE-IDENTICAL), 2 real tasks on 2 CPUs via the production .Ldispatch_loop (register-context switch). 5-dim audit PASS (byte-identity strongest-level, 1:1-conservation conversion, genuine 2-real-tasks); the ②.1b struct-collision merge was commander-reconciled (stack_top@56 + dispatch_disabled@64, sizeof 72, LD_PERCPU_BASE x72) + empirically validated (all cert families green). HONEST: mechanism only — production kernel runs on CPU 0, secondary timer/WAIT unwired (B parks on wfe), preempt still cooperative. **②.2b-i SHIPPED + audit MERGEABLE 2026-06-21 (a76f3606):** true async register-context preempt FROM IRQ context (the repo's hardest C-ABI piece — a tight no-poll task preempted mid-loop + resumes; 4-dim audit PASS) WITH the §5.4 BKL-held deadlock guard CERTIFIED by a real load-bearing [smp-no-deadlock] falsifier (WITH guard PASS, WITHOUT = permanent deadlock; NOT code-review-deferred — mk_pino: 諦めるのはNG) + 2 TOCTOU windows fixed; default byte-identical. **②.2c SHIPPED + audit MERGEABLE-WITH-NITS (nit fixed before merge) 2026-06-22 (eaa98f75) — ★ THE ARC'S PAYOFF:** the [smp-one-mind] CROWN — a REAL bare-metal mind forward (`r_forward`, 4-head Transformer, R_NP=21568) is BYTE-IDENTICAL (H_uni==H_smp==`0x2856a99b23880b4c`) whether run uniprocessor or scheduled as a real `tk_cre_tsk` task on an SMP secondary via the production .Ldispatch_loop (CPUs 2/3 = genuine concurrency) → "the mind stays one across the SMP scheduler." Default kernel byte-identical (755a20fa; smp_onemind.o excluded, r3_incontext.o 3edc0d07). Independent clean-room audit (24/24 stable, non-vacuity proven) found ONE nit — the `-DSMP_ONEMIND_RACE` falsifier was timing-flaky (~13%/boot spurious PASS → ~34% false BLOCK, never a false green); per 諦めない it was FIXED to deterministic-RED before merge (g_om_forward_inflight flag brackets r_forward's read window; commander-verified 20/20 FAIL, 0 spurious, 0 hang). HONEST: single-forward byte-identity; CONCURRENT minds race the shared static rc/rw[] → mind-lock DEFERRED. **②.2b-ii DESIGN HARDENED 2026-06-22 (97b89cdd, plan `smp-2b-ii-secondary-timer-plan.md`):** secondary CNTP timer + cross-CPU WAIT wake, 3-lens adversarial (crown/deadlock/WAIT). HEADLINE RISK surfaced: ②.2b-ii is the FIRST ② slice that must edit a DEFAULT-LINKED function (`knl_make_ready`, ~18 wake sites) — byte-identity preservable ONLY via a zero-token preprocessor macro (`knl_smp_wake_hook`), re-prove 755a20fa after the edit or STOP. **②.2b-ii SHIPPED + audit MERGEABLE-WITH-NITS 2026-06-22 (merge `201e6315`, impl `76f7c7b9`):** an SMP secondary is now a FIRST-CLASS scheduler — a CPU-1 task takes its OWN CNTP tick (tk_dly_tsk) AND blocks on a semaphore woken cross-CPU by CPU 0 (SGI→reschedule onto the certified ②.2b-i async hook). cert [smp-secondary-wait] 10/10 both halves; BOTH falsifiers load-bearing (NO_SEC_TIMER + NO_XWAKE RED — the impl caught + fixed a vacuous NO_XWAKE before the audit). **FIRST ② edit to default-linked shared core** (knl_make_ready via a ZERO-TOKEN macro) — crown HELD: `.text` 755a20fa + cpu_support.o `f3bad9a2` + task.o `f9ee9f21` cmp-clean base/HEAD (auditor 5× + commander on merged trunk). NIT (non-block): `-smp 8` flake is pre-existing+environmental (QEMU 8-CPU PSCI bringup; reproduces on base); knl_taskindp per-CPU-ization → ②.3. **REMAINING: ②.3** (finer locks + knl_taskindp per-CPU + general affinity/task-migration) -> hosted-port SMP (the teacher LLM's real multicore) -> RPi3 [live] (the only place barrier/SMPEN/race + BCM2837 per-core timer teeth bite). FOLLOW-UP: run_smp0.sh rebuilds kernel.elf in the boot dir (snapshot before boot); the -smp 8 environmental flake. Per-wave impl->audit.
- (idle — SS-6 ✅ merged AND **SS-6-`[live]` ✅ CASHED (wave-ss6-live): a REAL 4-process cross-node student forward over `./relay`, byte-identical to single-node, 266 experts on peers over the wire**. Next per the harsh-review pivot: keep cashing ONE thing at a time to `[live]`. Candidates: federation R0.1 (resp-scope + coord-crash-live), the SS-6 KV `kv_step`/live-chat wiring (the remaining SS-6 follow-up), or N-2c packet forwarding.)

## ✅ DONE THIS SESSION (on trunk `9e50e4a8`, NOT yet in an APK — next APK = 0.9.3)
> **HONEST FRAMING (2026-06-20 harsh review, finding #2):** most of this session shipped the SAFE half of each feature — a selector / a map / a seam / an `[in-proc]` cert — and DEFERRED the load-bearing distributed/learning half to a `[live]` row. The mind is not materially more capable; the scaffolding is. **The next win must be ONE thing driven to a real `[live]` N≥3 PASS, not another pure function.** (See QUEUE.)
- **KV cache** (`4c58a231`): O(1)/byte incremental gen, byte-identical to recompute cross-arch, 4.66→50x. ← a genuine capability gain (chat is faster). Audit PASS.
- **SS-3 cohort merge** (`7ec1ec54`) · **SS-5 placement** (`9e50e4a8`): same-tier weight-average + HRW expert→node MAP. **[in-proc] only — no expert executes remotely (SS-6); no blob crosses the wire (`[live]` deferred).**
- **N-2 selector** (`1f656c3c`) · **N-2b capability gossip** (`83f20dbd`) · **T-fix-a teacher selector** (`5270bbae`): lowest-id selectors + a gossiped capability bit. **No packet is forwarded by a supernode (N-2c); no lesson is transported/distilled (T-fix-b). SELECTION ONLY.**
- **§7 G38.0** (`9f7a9bc4`): a behaviour-PRESERVING `moe_select_step` extraction. **Moves no digits; the gacc learning is ON HOLD for mk_pino.** Value = a tripwire seam, not a feature.
- **self-access R0** (`d730175e`): READ-ONLY `body` introspection + Q3 lineage (1/invocation, tamper-evident). ← a real, complete capability. Audit PASS.
- **interoception slice-1** (`9e5529a2`): the unified stress **S_n bus** (real sources: reflex-threat/SWIM-RTT/surprise/fault/degrade) → DMN idle-threshold modulation + galaxy me-star real mood. ← real, the one live consumer is `dmn.c`. Audit PASS. Apoptosis (slice-2) deferred.
- **Design plans** (`survival-g38-impl-plan.md` §7 gacc · `thread-t-impl-plan.md` teaching · `federation-r0-plan.md`): cert-first plans that each found a real bug — **but in code that is NOT built (motion → a doc, per finding #3). Not defended behaviour.**

## ▶ ORDERED QUEUE (the marathon)

### Thread N — Network = the "Skype-like" P2P (mk_pino's passion; verdict SOUND)
- **N-0 ✅ SHIPPED** (commit 85017bb4: distinct, stable per-install node-id — no more collide-on-1). [was the HARD PREREQUISITE]
- **N-1 LAN-direct ✅ TRANSPORT SHIPS** (commit b68b845f: net_lan.c, both arches + all 3 build lists + PKERNEL_LAN dispatcher; swim beacon already broadcasts). **GAP = the [lan-direct] CERT** (no self-test today; design `n1-lan-direct-plan.md`); the real [live] = 2 machines on the same WiFi (the sandbox+ThinkPad 2-machine debug env, same /24, no netns/sudo needed). ~~`net_lan.c`~~ (3rd transport backend behind PKERNEL_LAN=1; net_unix.c template, 127.0.0.1→255.255.255.255:7351 + learned-peer table; SWIM's broadcast beacon becomes real LAN UDP). → **two same-WiFi phones auto-mesh, NO relay.** Host-first (2 namespaces/veth; SO_REUSEPORT caveat), then Android (CMake TU + parity). [net layer]
- **N-2 emergent supernodes** (relay forwarding logic lifted into capable nodes, NOCENTRAL deterministic-from-SWIM). ✅ **N-3 NAT hole-punching (in-proc gate) SHIPPED + audit MERGEABLE 2026-06-24 (merge `c125379f`):** supernode-brokered cone-NAT rendezvous — pure np_classify (cone/symmetric) + np_decide (PUNCH iff both cone, fail-closed) + np_broker_swap; NP_* packets ride SNF_PORT via the extended snf_rx switch; punch-timeout/symmetric/no-broker → N-2c relay fallback (connectivity never lost). cert `[nat-punch]` 17 PASS + sabotage `-DNP_SABOTAGE_CLASSIFY` → 14/3 (load-bearing); N-2c 20 PASS; crown 755a20fa exact. **cone-NAT ONLY (symmetric stays relayed — a real bound); real NAT traversal = a DEFERRED two-NAT [live] row.** ✅ **N-4 bootstrap/seed SHIPPED + audit MERGEABLE-WITH-NITS 2026-06-24 (merge `5041325a`):** PKERNEL_SEED list demotes the relay to ONE optional seed (NOCENTRAL); pure `seed_select_next()` + net_dispatch gate (engages at runtime) + solo-degrade (no usable seed → boots solo, exit 0, no hang); cert `[seed-bootstrap]` 9/9 PASS + falsifier `-DSEED_NO_ADVANCE` RED; back-compat byte-identical; crown 755a20fa exact. Cross-host join = DEFERRED [live]. **NIT→follow-up ✅ CLOSED 2026-06-24 (merge `d20a87c5`, impl `c4b67845`, audit-trail "resolve_relay degrade-not-crash"):** an unresolvable hostname (`garbage:notaport`) used to SIGSEGV in the shared resolve_relay; now `resolve_relay` no longer calls getaddrinfo (non-numeric host → logs `unresolvable host … — skipping` + returns -1; falsifier-only `RESOLVE_NO_GUARD` keeps the crash path), `parse_relay_list` drops the entry, an all-unresolvable list → solo/loopback degrade (never a crash); cert `run_resolve_crash.sh`, x86_64 parity, hosted-only/crown-safe. Doc: p2p-overlay.md.
  - **N-2 slice 1 DONE** (wave-n2-supernode-select): the deterministic **selection function** `region_supernode()` in `arch/common/region.c` (lowest-id node that is BOTH a region member AND supernode-capable; `0xFF` = degrade to central relay) + a local capability table/setter + `PKERNEL_SUPERNODE=1` self opt-in + host cert `region_supernode_test()` (verb `region test`; 8/8 PASS: lowest-capable-wins, convergence/determinism, survives-death by recompute (no vote), relay-fallback, non-member-capable ignored). Integer-only → cross-arch identical.
  - **N-2 slice 2b DONE** (wave-n2b-capability-gossip): SWIM **capability-bit gossip** — the self-declared capability rides each node's own ALIVE gossip in the `_pad`→`capability` byte of `SWIM_GOSSIP_EVT` (wire byte-identical, `SWIM_VERSION` unchanged; old nodes emit 0 → relay-fallback). Self-authoritative origination, verbatim relay, applied under the existing `(incarnation,state)` last-writer-wins gate so it converges in lock-step with membership and never regresses on a stale rumor. `region_supernode()` math untouched; the table is now fleet-real. Host cert `swim_cap_gossip_self_test()` (verb `nodes cap`; converge/staleness/falsifiable, cross-arch PASS, no swim/region regression). **N-2c [in-proc] SHIPPED + audit MERGEABLE 2026-06-21 (commit `9c3a1c52`, audit-trail row "N-2c"):** the elected supernode actually FORWARDS A→B; route decision `snf_route_target` pure+fail-closed, 20/20 cross-arch, byte-identical forward, falsifiers degrade correctly. **N-2c `[live]` is the ONE genuinely-OPEN row (audit-trail).** Root-caused on the real ThinkPad host 2026-06-23: NOT harness timing (PRESEND 11s→30s did not fix it) — the relay overlay is a BROADCAST medium (`net_relay_send` only emits REL_BROADCAST), `udp_send()` drops on first-time ARP miss (`netstack.c:101`), and `snf_send` does a SINGLE fire-and-forget udp_send with NO retransmit, so A's first SNF_FWD to S is silently lost. `ss6_live.c` survives the identical race via `SL_RETRIES 3 × SL_WAIT_MS 200`; `snf_send`'s own comment admits "a full ACK/retry is a deferred hardening." **N-2c `[live]` CLOSED 2026-06-23 (merge `321814ca`, impl 1b0eeba4):** fix = the deferred ACK/retry hardening (A retransmits SNF_FWD 3×200ms keyed by a new `SNF_PKT.seq`=_pad2, wire 524B unchanged+pinned, blocks until end-to-end SNF_ACK B→S→A, timeout→DIRECT no loss, `(origin,seq)` dedup exactly-once) + delivery-time self-report (real byte compare, survives `[moe]` console spam) + harness id-map fix (`my_supernode=1` internal, `snf send 2`) + convergence wait. **REAL-HOST [live] PASS on the ThinkPad** (3 OS procs + ./relay): MAIN forwarded via_super=1 BYTE-IDENTICAL acks=1; no-supernode→DIRECT; killed-S→fail-closed DIRECT no loss. in-proc 20/20 ×2 + sabotage-RED; crown 755a20fa exact (supernode.c hosted-only). Audit MERGEABLE-WITH-NITS (8/8 axes). **STILL DEFERRED:** NAT hole-punch (N-3), seed bootstrap (N-4); runtime capability flip (needs incarnation bump).

### Thread M — the special-structure MIND = 完全体 (special-structure-mind.md)
- ~~**SS-3** same-tier merge cohorts~~ **SHIPPED 2026-06-19 (wave-ss3-merge-cohorts):** student-only `st_merge_cohort` (peer-symmetric, canonical reduction, one-math) + `st_blob_tier_ok` cross-tier refusal (islands) + chunked p-fs transport `gl_student_publish`/`_fetch`; NEVER calls `gl_merge`/touches `rw[]`. Cert `tests/llm/run_ss3.sh` 6/6 (`[ss3-cohort-converge]` merged loss ≤ worse parent + byte-symmetric, `[ss3-cohort-island]`, `[ss3-merge-falsifiable]`, `[baby-merge-isolation]`+`[no-vla]`). HONEST: averaging is the mechanism (converges a SHARED objective; LOSSY for divergent facts — Path-W², out of scope); LIVE chunked publish/fetch over the relay is a DEFERRED `[live]` row (converge claim is in-process). **⚠️ 2026-06-26 — the [live] row is BLOCKED by a non-functional transport (verified, not just scoped):** `gl_student_publish`/`gl_student_fetch` (gossip_learn.c:202/228) save **one NAMED p-fs ref per 4096-B chunk** (`st/<node>/<c>`), but `PFS_REF_MAX=16` (pfs_dag.h:62) → the smallest (S-tier) blob is 1,973,036 B = **483 distinct named refs**, so `pfs_dag_save` returns `PFS_E_FULL` at the 17th and publish returns -1. Both functions have **ZERO callers** (declared-only in gossip_learn.h) — never exercised by any cert; `GL_ST_MAXCHUNK=8192` self-contradicts the 16-slot table. Even with a bigger table, the `pfs/ref` gossip plane (3 names/800ms) needs ~128s (S) / ~25min (M) to advertise, and the P1 content-announce is a single clobberable LATEST_ONLY slot. **The "transport ships" premise was false — this is the harsh-review's "safe half shipped, load-bearing half is dead code" made concrete.** Doing SS-3 `[live]` requires a real C transport REDESIGN first (one named manifest ref + content-addressed chunks via pfs_repl_put/_want bypassing the nameplane; in-proc publish→fetch→`st_merge_cohort` round-trip cert BEFORE any harness; gossip_learn.c is BARE-METAL-linked → crown-sensitive). Recorded in audit-trail "SS-3 [live] transport — BLOCKED". **✅ DESIGN-DOC HARDENED 2026-06-26 (`docs/architecture/student-blob-transport.md`):** content-addressed transport (chunks→`pfs_repl_put` cid, 2-level index of ≤127 ids/block, descriptor under ONE named ref `st/<node>`; fetcher pulls by explicit WINDOWED `pfs_repl_want`) — collapses 483 names→1, depth-2 covers S+M; **`pfs_repl.c` needs NO change** (windowed want paces itself, LATEST_ONLY sidestepped) so the entire fix is hosted-gated in gossip_learn.c under `_TK_HOSTED_LIBC_` with a byte-identical bare-metal `#else` → crown 755a20fa preserved; in-proc `[ss3-blob-roundtrip]` cert (SOLO, memcmp==0→st_merge_cohort + drop-chunk falsifier) gates the transport BEFORE any [live] harness; biggest risk = the [live] want-storm/convergence-time (mitigate with ss6-style retransmit + progress-reset retry). **✅ STEPS 1+2 SHIPPED + INDEPENDENTLY AUDITED MERGEABLE 2026-06-26 (merge `e655539d`, impl `43b2ad3f`, audit-trail "SS-3 student-blob transport (steps 1+2)"):** the content-addressed transport is rewritten hosted-gated + the in-proc cert `run_ss3_blob.sh` 12/12 PASS (a 482-chunk depth-2 S blob round-trips memcmp==0 into a separate buffer → real st_merge_cohort, merge-of-transported==merge-of-direct bytes; falsifier PROVEN load-bearing — auditor disarmed the drop → 10/2 FAIL). Crown aarch64 `755a20fa` + x86 `4064d8a9` byte-identical (re-derived by auditor AND commander on trunk; #else byte-for-byte, nm shows zero new bare-metal symbols); pfs_*/student.c untouched. **AUDIT-CONFIRMED FINDING (design §2.4):** the in-memory P0 store is `PFS_MAX_BLOCKS=64` < 482 chunks → every receiver of a multi-MB blob must mount the ARK durable backend or drop blocks at #65 (the cert does; step-3 [live] must). **REMAINING: step-3 [live] 3-proc relay harness** — host-heavy + a prioritization call for mk_pino (vs federation F1); not launched autonomously.
- ~~**SS-5** deterministic expert placement map~~ **SHIPPED 2026-06-20 (wave-ss5-placement):** NOCENTRAL local function `st_expert_owner(expert_id)` = HRW (rendezvous-hash) winner of (expert-id, alive-member-set), computed locally from the SWIM `dnode_table[]` view — no broadcast/vote/leader/new-gossip; re-derives on membership change (dead node's experts re-home, all others UNCHANGED — minimal disruption). New module `arch/common/placement.c`/`.h` (kernel tier, builds on all four boots); a THIN shim REUSING `lookup_responsible()` (`arch/common/lookup.c`) — stable per-expert sha256 key ⇒ byte-identical cross-ABI, integer+sha256 only (no float). Cert `tests/llm/run_ss5.sh` 8/8 + shell verb `place` (`[place-deterministic]`, `[place-rehome]` w/ falsifiable modulo-N control 8 vs HRW 4, `[place-balance]`, `[nocentral]`/`[no-float]`/`[no-vla]`). HONEST: placement MAP only — remote-expert EXECUTION is **SS-6 (DEFERRED)**; single-node = all experts → the one node (the map is SS-6's foundation); a multi-node LIVE convergence run is a DEFERRED `[live]` row.
- ~~**SS-6** cross-node expert firing with local fallback~~ **SHIPPED 2026-06-20 (wave-ss6-cross-node):** the F4 capstone — "複数ノードをまたぐ一回の forward". When the SS-1 adaptive router WIDENS K beyond K_min on a hard token, the EXTRA experts (chosen-slot j ≥ K_min) that SS-5 placement puts on a PEER are computed REMOTELY (the peer runs that expert's SwiGLU on the `[D]` f_in vector, ~D floats on the wire) and their `[D]` outputs are summed into `moe[]` in a FIXED canonical reduction order (ascending slot j == the single-node order — CRITIQUE GATE #3) ⇒ a remote forward equals the single-node forward BYTE-FOR-BYTE. Both branches call the SAME `st_expert_forward_ref` (one math). The local K_min experts ALWAYS run locally; a remote timeout/absent peer RECOMPUTES that expert locally (lose the WIDTH, not correctness — honest degraded). Transport is the caller-installed `st_set_remote_expert(fn, gate, ctx)` hook (kernel wires DRPC; cert wires a stub) — no new transport invented. New API in `student.c`/`.h`; the MoE loop materializes per-expert `[D]` outputs into a file-static `eo_all[KMAX][DMAX]` (no-VLA, no stack-overflow) then folds canonically. Crown: NEVER touches `rw[]`/`gl_merge`; KV byte-identity intact (kv_step untouched, run_kv still byte-identical); single-node path byte-unchanged when the hook is clear. Cert `tests/llm/run_ss6.sh` 7/7 (`[remote-expert-equiv]` M 506 / L 940 experts remote, byte-identical logit hash; `[remote-expert-fallback]` timeout → local recompute, still byte-identical; `[remote-falsifiable]` 1e-6 perturbation FAILS; `[canonical-order]`/`[no-vla]` greps clean). S tier proven INERT (E=2==K_min, no widening, no fan-out). HONEST: in-process cert drives the REAL canonical-sum + fallback path with a STUB peer; a true multi-process forward over the relay is a DEFERRED `[live]` row; the hook is wired into `st_forward` (the canonical forward) — wiring it into the KV incremental `kv_step` generation path is a documented follow-up.
- ~~**SS-6 `[live]`** the multi-process cross-node forward~~ **SHIPPED 2026-06-20 (wave-ss6-live):** the DEFERRED `[live]` row is CASHED. A REAL relay-backed remote-expert transport (`arch/common/ss6_live.c`/`include/ss6_live.h`, UDP port `SS6L_PORT`=7378) mirrors `drpc.c`'s pending-table + reply-semaphore + hard timeout (3×200ms→fallback); the requester ships the `[D]` `f_in` to the SS-5 `st_expert_owner` (HRW, NOCENTRAL — no vote) over the mesh, the owner runs the EXACT `st_expert_forward_ref` and replies the `[D]` output, and `student.c` sums it in the UNCHANGED canonical ascending-slot order. Installed via `st_set_remote_expert` behind a fail-closed gate (`PKERNEL_REMOTE_EXPERTS=1` AND wide slot j≥K_min AND `!st_expert_is_local` AND `degrade==FULL` AND `region_size()≥2`) so a default single node is byte-unchanged and training stays local. Cert `samples/11_distributed/run_ss6_live.sh`: **4 p-kernel PROCESSES over `./relay`, degrade FULL; node 1's LIVE cross-node forward logit-hash == its single-node oracle hash (`00d6136e6d4676a3`), 266 experts computed on PEERS** (266 REQ + 266 REP `[D]`-vector frames on the relay log, sizes 1114/1118 B). Killed-peer arm `run_ss6_live_fallback.sh`: a peer killed but still ALIVE in placement → 4 experts time out → recomputed LOCALLY → STILL byte-identical, `fallback=4`, no stall (honest degraded). NEVER touches `rw[]`/`gl_merge`; **`run_kv` still 18/18 byte-identical (`kv_step` UNTOUCHED)**; `run_ss6` 7/7 unchanged; ss1/ss2/ss5 + dkva/placement self-tests PASS. Built on all four boots (`ss6_live.c` hosted-only in `COMMON_C_SRCS`; not on bare metal). HONEST: the `[live]` forward reaches `st_forward` ONLY — the LIVE CHAT / KV `kv_step` generation path is NOT wired ("live cross-node forward" ≠ "live chat is distributed"); ran 4-process on ONE host (single-host floor; the authorized SSH 2-machine run not attempted this wave); the cert model is M-tier (L overflows the small shell-task stack — L byte-identity already proven in-process by `run_ss6`).
- **SS-4** function-preserving expert growth (cap_experts_of(N) sizes the router) — **SHIPPED + audit MERGEABLE 2026-06-22 (`7e358bac`, plan `ss4-function-preserving-growth-plan.md`):** the independent auditor went BEYOND the cert (40 diverse + 4000 random inputs = 0 drift, deep incumbent-weight/Adam bit-compare clean, SS-6 hashes `63e8de33`/`67f2434f` unmoved). grow `m->nexpert` (student.c:248) by adding a DEAD expert (router row=0, W2=0, AND an `alive[]` mask = effective logit −∞ so it's never in the chosen set) → EXACTLY function-preserving (ε=0, ALL inputs) because this router is top-K-then-softmax-over-chosen-set + margin-widening (NOT global softmax → the textbook clone recipe is WRONG here, design corrects it). Specialization = a SEPARATE later "resurrection-by-cloning" event (deliberately ε, gated by a looser `[grow-then-learn]`, NOT the exact cert). Crown 0x2856a99b/755a20fa untouched (r_forward has no expert axis). cert `[expert-growth-preserves]` (logits FNV unchanged; "nk/firing-width unchanged for EVERY token" is the tripwire — the −∞ mask, not router=0 alone, is what makes it exact) + falsifier naive-random-init. Mixed-size minds cohort by (tier,nexpert), bridge by distillation not cross-size merge. IMPL re-verify SS-6 hashes `63e8de33`/`67f2434f` (both arches, -O1 -ffp-contract=off) or STOP. · **SS-7** bigger baby. [SS-4/SS-7 both edit student.c — SERIAL]

### Thread T — conversational TEACHING (education 考え方 → ✅ cert-first PLAN DONE: `thread-t-impl-plan.md`)
- ✅ **cert-first impl PLAN DONE** (design-harden workflow): winner = **text-level** distillation (teacher emits TEXT → byte-native student learns bytes directly; builds on the REAL distill path `distill_proof.c` proves weight-resident); transport = KDDS `CRADLE_TEACH` beacon + p-fs body; teacher-selection = **bit 1 of the N-2b SWIM capability byte** (wire-compat, unanimous). Critique caught a real cert bug (positional held-out split → a naive probe cert can FAIL with teaching ON) → corrected cert separates a trained lesson copy from a never-trained held-out probe (= generalization, not rote). Soft/logit distillation DEFERRED behind a `fmt` byte. Depends on Thread N (mostly done) + the teacher engine (exists).
- ✅ **T-fix-a DONE — NOCENTRAL teacher SELECTION** (`wave-tfix-a-teacher-cap`): mirror of N-2b. SWIM `capability` **bit 1 = teacher-capable** (Option A; wire UNCHANGED — `SWIM_GOSSIP_EVT` 4B / `SWIM_PKT` 24B / `SWIM_VERSION` 1, asserts hold; old nodes emit 0 → supernode=0,teacher=0 safe degrade). `teacher_self()` (swim.c) gates bit 1 on a VERIFIABLE loaded teacher GGUF via a **weak `teacher_gguf_loaded()` hook** (strong override in student_shell.c does a one-time cached `gguf_open`+`lm_load` probe, env `PKERNEL_TEACHER=1` necessary-not-sufficient); weak default 0 covers bare-metal/Android (safe degrade). Bit-packed at both origination sites + applied under the SAME `(incarnation,state)` LWW gate (stale rumor cannot flip it). `region_teacher()`/`teacher_capable[]`/`region_{set,is}_teacher_capable()` (region.c) = lowest-id node BOTH member+teacher-capable, 0xFF if none, survives teacher death by recomputation. Cert `swim_teacher_gossip_self_test()` (`nodes teacher`) PASS converge/selector/determinism/staleness/falsifiable (RED when apply ignores bit 1); `region test` adds `[region-teacher] 5/5`; no regression (`[cap-gossip]`/`[swim-incarn]`/`[region-super] 8/8` green). 4-build clean (linux, linux_x86_64, x86, aarch64), cross-arch cert green under qemu. **HONEST: SELECTION only.**
- ✅ **T-fix-b/T-1 — the lesson BRIDGE — SHIPPED + audit MERGEABLE 2026-06-21** (commit `d97ced2d`/cherry-pick, impl `0f8997f5`, audit-trail row "T-fix-b / T-1"): a teacher's TEXT lesson, pulled by the student over the KDDS `CRADLE_TEACH` beacon + p-fs DAG body (`cradle_net.c`), trained → a NEVER-trained held-out PROBE drops 5.5220→1.8358 (generalization, not rote); 3 falsifiers RED + sabotage-proven (teaching-off / scrambled-bytes / teacher-death), byte-identity crown intact (run_kv 18/18, student.c math untouched), NOCENTRAL (no gl_merge; local `region_teacher()`). **DEFERRED [live]:** the true multi-process live teacher-convergence over ./relay + the in-kernel live GGUF teacher harvest (CT-2) + `LESSON_FMT_SOFT`.
  - **🚩 cradle-live [live] IN PROGRESS 2026-06-23 — and it FOUND A REAL BUG.** The deferred multi-process teacher-convergence [live] row: harness `samples/11_distributed/run_cradle_live.sh` (3 procs T+S+W+relay) + hosted `cradle probe`/`off`/`emit-scramble` verbs + cert-scoped `PKERNEL_TEACHER_CERT` SHIPPED (merge `69a69387`). Running it on the real ThinkPad EXPOSED a latent **production** bug the in-proc cert had masked: a teacher node broadcast its capability bit but **never self-applied it to its own `teacher_capable[]` table**, so `region_teacher()==self` was never true on the teacher → `cradle_teach_emit` never fired live → live teaching was structurally broken. **FIXED + audit MERGEABLE (`c81628a0`, audit-trail "teacher SELF-ELECTION fix"):** `SELF_APPLY_OWN_CAPABILITY()` at the 2 self-beacon sites (hosted-gated, crown 755a20fa byte-identical), regression cert `nodes selfelect` + falsifier `-DSELFELECT_SKIP`. **The [live] CURE-arm green is OPEN — pending the ThinkPad re-run (mk_pino traveling, host unreachable); NOT fudged.** Exactly the 2026-06-20 harsh-review prediction: the in-proc "safe half" hid a bug only the [live] half exposes.
  - **🚩 2026-06-25 — the self-election re-run went TWO LAYERS DEEPER and found two MORE in-proc-masked bugs, both FIXED + independently audited MERGEABLE + integrated (crown 755a20fa re-derived byte-identical).** **L1 — T-fix-c lesson-format** (merge `a8fce20f`, audit-trail "cradle-live L1"): the live lesson body was ~115 B < `CRADLE_MIN_LIVE`=128 (the harness's 1280-B string was truncated by the kernel shell line-buffer when passed as a `cradle emit <text>` arg), so `cradle_lesson_ingest` refused it (ring stayed 0). Fix = a new `cradle emit-canon` verb composes the lesson IN-KERNEL via `cradle_compose_canon()` (byte-identical to the cert's `ct_build_lesson`, 1280 B), bypassing the line buffer; ingest unit + in-proc cert (MAIN drop 3.69, 3 falsifiers RED) + over-the-wire beacon len=1279 all green; generalization intact (trainer [0,960) disjoint from probe [960,1280)). **L2 — DMN-stack** (merge `ba450a02`, audit-trail "cradle-live L2"): the student's `st_forward`/`st_backward` math overflows an 8 KB task stack, so the autonomous DMN sleep (`dmn_task`, prio 13) + `galaxy_task` + the init/shell task CRASHED (EXIT 139) the moment they trained — latent because every prior living-mind cert drove the math from a host binary's 8 MB stack, never the in-kernel 8 KB task. Fix = 256 KB for the three hosted tasks that reach st_forward (matching the SS-6 precedent); auditor exercised the REAL prio-13 dmn_task idle→`[dmn] sleep: distilled`→loss 5.5409→2.9286, exit 0, no fault (parent crashes 139). **The living-mind's DMN sleep can now actually run on a real in-kernel node.** **L3 — beacon-vs-ref race** (commit `d8eb6710`, audit-trail "cradle-live L3", audit MERGEABLE): with L1+L2 the lesson reached S but a fresh S still never ingested it — the LATEST_ONLY beacon outran the lossy/lagging "pfs/ref" gossip, so `pfs_dag_read` of the newest `ct/<t>/<seq>` returned NOTFOUND (S lacked that ref binding) though it HAD earlier resolvable refs + the identical content. Crown-safe fix (cradle_net.c hosted; the ref-gossip lives in bare-metal pfs_dag.c/pfs_repl.c, NOT touched): on the newest ref failing, scan seq downward to hw+1 and pull the NEWEST RESOLVABLE seq (+ an honest OFF-gate restoration). **CORE FLAGSHIP PROVEN:** `fallback: pulled resolvable seq=6 (beacon seq=11)` → `ingest ring_len=1279` → the autonomous DMN consolidates → held probe **5.5915→2.6025** (~2.99 nats below chance, generalization intact); falsifiers honest (OFF ring 0/chance, SCRAMBLE ring-fills-but-stays-chance, DEATH still-answers); crown 755a20fa re-derived byte-identical. **A fresh student learns a relay-delivered fact across the wire.** **FORMAL MULTI-NODE VERDICT NOW GREEN (host-independent)** — autoprobe-cert (merge `6d491756`, audit-trail "cradle-live autoprobe-cert", independently audited MERGEABLE): the cure/scramble/death arms previously forced an explicit `baby 16` (16 sync rounds over a ~247MB student) that timed out the 180s cap on the cooperative-single-core PRoot host (post-probe starved) — a host-speed artifact, NOT a fix failure. The shell-only fix certifies on the student's OWN AUTONOMOUS DMN idle probe (the production sleep) instead: VERDICT `PASS held probe 5.5915->2.6025 over the wire; teaching-OFF / scrambled stayed at chance; the mind survived the teacher's death`, DONE_RC=0 (CURE 2.6025/ring1279, OFF ring0/5.5452, SCRAMBLE ring1280/5.8184≥chance, DEATH post-kill 2.6025). The auditor refuted the fallback-masking concern at source (single emit site `student_shell.c:674`; monotone training ⇒ `tail -1` reads the worst-case for the scramble assert ⇒ conservative) and witnessed its own clean GREEN first-try; thresholds byte-identical to parent; crown shell-only/untouched. **REMAINING (re-confirm only, NOT a gate):** a genuine real-NAT / real-ThinkPad re-run (the earlier `baby 16` harness loaded the ThinkPad and it went SSH-unreachable mid-run; mk_pino to clean stray procs).

### Thread B — the 🅰 big designs (each its own multi-wave)
- **interoception / 負のエネルギー**: ✅ slice-1 DONE (`9e5529a2`: S_n bus → DMN modulation + galaxy hue). **slice-2 apoptosis DESIGN HARDENED 2026-06-22 (`1221fce4`, plan `interocept-2-apoptosis-plan.md`):** a dying node hands its ESSENCE (rw[] weights + diagonal Fisher + Self-lineage tip) into the collective via Path W² BEFORE death (signed ESSENCE over KDDS `mind/w` → heir verifies `sign_manifest_verify` 3-gate → `gl_merge_w` + union-replay to bar=75% → ACK → node exits ONLY after ACK, fail-closed). Apoptosis = the FIRST live use of Path W² (today `gl_merge_w`/`r3_fisher_diag` fire only in the LM-11 cert; live `mw_fold_region` still uses W¹). cert `[apoptosis-essence-survives]` (heir provably ignorant of the fact → only the handoff explains survival) + falsifier `[apop-before-death]`; crown 0x2856a99b/755a20fa unaffected. **⛔ IMPL BLOCKED ON MK_PINO'S PHILOSOPHY DECISION — do NOT implement autonomously:** the soul-level question **B-3 = is apoptosis VOLUNTARY-ONLY, or may the collective "euthanize" a sick node?** (a majority that can kill an individual = the seed of ownership/control, against "a home no one owns"; design's recommended default = voluntary-only, euthanasia deferred). +4 other his-call questions (consent holder, single-heir vs broadcast, provenance-named vs anonymized, right-to-be-forgotten). Biggest tech risk: cert vacuity via passive shared-mind replication (mandate: run the negative control). Follow-up: per-axis normalization bands from measured curves (§2.4). (interoception.md)
- **self-access / 自分の体を触る**: ✅ R0 read-only introspection DONE (`d730175e`). **REMAINING: R1** the mind invokes `body` AUTONOMOUSLY (DMN idle hook) → **BLOCKED on user decision Q1** (how free is autonomous read-only) **/ Q2** (driver consent once vs each); then drive own shell/storage/devices → self-write+compile drivers. Q3=YES already (log body-touch to lineage). (project_self_access_embodiment)
- **imaginary-UI**: conversation conjures ephemeral GUIs on demand ("電卓が欲しい"→builds one). (web-os.md)
- **compatibility / 凍結なし進化** (DECIDED 2026-06-14): generational succession + per-version migration chain + signed-OTA. **DESIGN HARDENED 2026-06-22 (`cd773ed1`, plan `compat-migration-chain-plan.md`):** 4 version axes (wire/on-disk/mind-format/build); migration chain = per-axis `vN→v{N+1}` registry composing for multi-step catch-up (generalizes the LIVE `LM_SELF_ENTRY` dual-width walker v1=116B→v2=148B); invariant read-old→upgrade→refuse, never silent corruption; mind-FORMAT changes migrate by RE-EDUCATION (Path E) not weight-copy; signed-OTA = 4-gate AND reusing `sign_manifest_verify` + legal-successor/no-downgrade. 3 certs `[migrate-forward]`/`[signed-ota-gate]`/`[no-fleet-split]` w/ falsifiers. Biggest risk = arkfs deep-gap (crash-safe LOG `super_valid` clean-rejects on version → reject not migrate; v1 may ship reject+reformat for arkfs while flat blobs truly migrate). **FIRST IMPL SLICE SHIPPED + audit MERGEABLE 2026-06-22 (`ac9ed78e`): `[migrate-forward]` for the R3 weight blob `R3_WP`** (one real v1→v2 step adding the `merge_epoch` header field + back-to-front payload shift; taught-fact 88.3% v1 blob → wipe 0% → migrate → 88.3% lossless; falsifier `-DR3WP_SKIP_MIGRATE` → width-mismatch → fact lost 0% → RED; production load re-validates payload sha256 post-migrate). R3_WP is HOSTED-ONLY (`#ifdef _TK_HOSTED_LIBC_`) so crown structurally safe — `.text` 755a20fa + `r3_incontext.o` 3edc0d07 cmp-clean base/HEAD (auditor + commander). **`[signed-ota-gate]` SHIPPED + audit MERGEABLE-WITH-NITS 2026-06-22 (merge `63032e06`):** `compat_ota_accept` = 4-gate AND (shipped `sign_manifest_verify` 3 gates + artifact_ver>running_ver); accept good successor, REFUSE {tampered/non-adopted-key/downgrade×2/relabel} each at the right gate; falsifier `-DOTA_SKIP_VERIFY` RED; **9 adversarial forge attempts all refused** (version is unsigned-no-wrap + cryptographically in the signed body); sign.c reused VERBATIM (unweakened), human-identity boundary HELD, hosted-only (crown 755a20fa safe). **`[selflineage-migrate]` SHIPPED + audit MERGEABLE 2026-06-23 (merge into wave-i18n-galaxy, impl `6b85d22e`):** the Self-lineage hash-chain leg — the existing dual-width v1(116B)→v2(148B) `LM_SELF_ENTRY` walker formalized as an `lm_self_steps[]` chain; COHERENT whole-chain migration re-links each entry's `prev_entry` to its migrated predecessor's NEW v2 content-id so the hash chain verifies end-to-end under the UNMODIFIED production `self_walk` (len 4/4); falsifier `-DLMSELF_SKIP_MIGRATE` → re-walk len 1/4 (re-link is the sole load-bearing gate); hosted-only, crown 755a20fa byte-identical (6/6 adversarial checks). **`[x86_64-cert-parity]` SHIPPED + audit MERGEABLE 2026-06-23 (impl `343466b4`):** `EXTRA_CFLAGS` knob threaded into `boot/linux_x86_64/Makefile` + the gated `compat test`/`compat test ota` verb mirrored into x86_64 usermain → x86_64 hosted now COMPILES AND RUNS build-time certs end-to-end ([migrate-forward] PASS native+qemu, falsifier RED, [signed-ota-gate] PASS); cross-ABI cert parity is REAL; crown safe. **`[no-fleet-split]` SHIPPED + audit MERGEABLE 2026-06-23 (merge `b2ea2301`, impl d78eaeb8):** in-proc `swim_nofleetsplit_self_test` drives the REAL `swim_rx()` — a vN and a v{N+1} ADDITIVE-change node (capability byte, no SWIM_VERSION bump) stay ONE fleet; a BREAKING version bump (`version!=SWIM_VERSION` → hard-drop swim.c:418) partitions. 4 legs; sabotage of the swim.c:418 version clause flips ONLY the partition leg RED→revert green (load-bearing). flag `-DNOFLEETSPLIT_CERT`, verb `compat test wire` (both usermains, x86_64 PASS under qemu); crown 755a20fa exact. HONEST: does NOT retire compatibility.md §7 (replica.c still hard-drops — reject→degrade is separate); [live] 2-process teach→answer deferred. **`[arkfs-version-gap]` SHIPPED + audit MERGEABLE 2026-06-23 (merge `88fb2cf1`, impl 5bade35f) — ✅ THE COMPAT MIGRATION-CHAIN THREAD IS COMPLETE.** Decision: the crash-safe LOG ships clean REJECT+REFORMAT (NOT in-place migration; valid-foreign super → no replay → `ark_format` epoch-bump → boot clean, one honest emit; node survives via Self-lineage + Path-E — arkfs holds backing bytes, not identity). Policy in the hosted `pfs_ark.c` caller; arkfs.c changes ALL hosted-gated (super_valid falsifier behind `ARKFS_GAP_SKIP_VERCHECK && _TK_HOSTED_LIBC_`, `#else` preserves bare-metal verbatim; `ark_super_version_peek` under `_TK_HOSTED_LIBC_`) → bare-metal crown 755a20fa exact. cert `[arkfs-version-gap]` (hosted-only TU): cure PASS (+1/-1 sub-cases + negative-space old-payload-GONE); falsifier `-DARKFS_GAP_SKIP_VERCHECK` RED (foreign log replayed). +x86_64 `compat test self` parity fold-in. **Compat thread = {R3_WP + Self-lineage truly migrate; SWIM wire no-fleet-splits; signed-OTA refuses bad updates; arkfs reject+reformats} — all falsifiable, never silent-corrupts, crown never moved.** Honest: OTA-ACCEPT gate only — delivery/transport + key revocation (CRL) deferred (A/B rollback = the only recovery). (compatibility.md §DECISION, project_compat_evolution)
- **federation** 254→10k staircase. ✅ **R0 SHIPPED 2026-06-20 (wave-fed-r0):** the 2-cluster DKVA hierarchy is now **certified, with the `[live]` arm actually running on the real multi-process harness** (the 2026-06-20 review's pivot demand). Load-bearing verify DONE: `rsum/<rid>` IS `KDDS_SCOPE_GLOBAL` while `resp/<n>` is `KDDS_SCOPE_REGION` (`dkva.c:809-818`) → the O(#region) property holds (the plan's grepped-line worry was a false alarm). Slices: (1) `dkva_fed2_self_test()` `[fed-2cluster][in-proc]` — production expect-loop refactored into a pure core `dkva_expect_core` driven by both `dkva_infer` AND the test; falsifier `rc_cnt0==1` (O(#region) not O(#nodes)) + summary==dense fold + NOCENTRAL min-id; (2) `[coord-crash][in-proc]` — b1 dead → rep deterministically b2, no election, expect-set re-forms; (3) `[fed-2cluster][live]` in `run_4node_regions.sh` — **8 OS processes through `./relay`**, diffs `[locality]` counters across one infer + a **flat-control falsifier** (`PKERNEL_RTT_ZONE_PENALTY=0`): zoned far=6/near=3 (summary-only crossing) vs flat far=0/near=9 (direct fan-in to all N) → mechanically distinguishes hierarchical from degenerate. **All arms PASS.** NO DNODE_MAX/wire change (the 256-wall raise = a separate F1 wave). `federation.md` §1/§5 stale text corrected. HONEST: `[live]` ran 4-node/2-zone on ONE host (SSH 2-machine not attempted); 10k-dream gap (256 wall, MTU, array fan-out) stays wide open — R0 is the first tread, not the staircase. Audit PASS (independent reproduction 3/3 + broke-the-hierarchy-twice→RED; row CLOSE) — `docs/audit-trail.md`. **R0.1 IMPLEMENTED 2026-06-21 (wave-fed-r01, pending separate audit):** both audit-flagged items addressed. (1) **Responder-side locality gate** added to `run_4node_regions.sh`: nodes 3 (coordB) + 4 (non-coord responder) now print their OWN `[locality]`, so a `resp/<n>` REGION→GLOBAL degeneration that shows up on the RESPONDERS' TX (which node1 never sees) IS now dynamically falsified. New production hook `dkva_set_resp_global()` (default OFF → byte-identical; armed by `PKERNEL_DKVA_RESP_GLOBAL=1` on the responders only). Gates: R1 non-coord resp stays REGION (node4 near≫far, ~102≫4); R2 coordB dense resp crosses under falsifier; **R3 load-bearing falsifier** — resp=GLOBAL makes node4 far jump 4→~204 (the dense partial crosses) → gate RED. Stable 3/3 runs; the R0 requester gate + flat-control falsifier stay green (K bound 6→10 to absorb the longer-lived-responder background jitter). (2) **`[coord-crash][live]`** was ALREADY shipped at this base via `samples/11_distributed/run_coord_crash.sh` (commits `0fcad504`+`3215687d`): PART A real `kill -9` of the region coordinator → min-id re-delegation (node0→node1 via `region_recompute`, no election); PART B DKVA reconverges through the survivor with honest `degraded (k/n)` accounting, no hang/SPOF. Re-verified PASS with the R0.1 change (no regression). NOCENTRAL/deterministic/one-mind held (no new central authority; only `dkva_set_resp_global` symbol added; region.c/kdds.c/swim.c/world.c/raft.c untouched). HONEST floor: still single-host (8 OS processes in 2 sequential runs per harness; SSH 2-machine deferred).
- **survival-network「考える器官」**: §7 distributed gating (= regions R3 / mutual-aid) · §8 two-layer oscillation fix · decentralized whole-network situational-awareness map. (survival-network.md) — ✅ §7/G38 cert-first PLAN DONE (`survival-g38-impl-plan.md`) + ✅ **Wave G38.0 SHIPPED** (`9f7a9bc4`, audit PASS): the testable seam `moe_select_step` extracted from `select_expert` (behaviour byte-preserved; `[moe-seam]` cert closes trap A2 so a future learning cert can't be a rubber stamp). **REMAINING: G38.1+ the `gacc` local-gradient learning + §8 stabilizer** — ON HOLD pending mk_pino's review of his core §7 philosophy (the commander reads the gate formula line-by-line before crediting; highest-risk gate = the COUPLED pressure+gacc-sum oscillation proof). §8 map + survival slice-2 still queued.
- **r3-nontrivial-thought**: multi-step non-trivial reasoning. (r3-nontrivial-thought.md)
- **multi-core compute ③ — deterministic parallel matmul** (stepping-stone to ② full SMP; kernel stays uniprocessor). Doc: `multicore-matmul-plan.md`. The crown invariant = BYTE-IDENTICAL to serial for any worker count (one mind, one math; output-row partitioning never reassociates → admissible on the `rw[]` merge path, unlike the deferred GPU).
  - **MC-0 SHIPPED + AUDIT PASS 2026-06-20:** `pk_parallel_rows` primitive (`arch/common/llm/pk_parallel.{c,h}`, HOSTED pthread pool, `(out,NW)` partition w/ ragged-remainder-on-last, condvar-BLOCK idle = no busy-spin). `[par-matmul-equiv]` memcmp==0 + FNV equal for `NW∈{1,2,4,8}`; `[par-matmul-falsifier]` reassociating variant DIFFERS (teeth); `[mc0-idle]` ~0 CPU. NO speedup claimed.
  - **MC-1 SHIPPED + AUDIT PASS 2026-06-20:** size-gated (`pk_parallel_rows_gated`, gate=524288 MACs) on the teacher forward; **2.2–2.67×**; full forward logit hash byte-identical at every NW w/ real GGUF; small student + R3 stay inline (below gate).
  - **MC-2 design plan** (`mc2-baremetal-smp-plan.md`) — bare-metal constrained SMP. HONEST: bare metal has NO big matmul today (LLM tier hosted-only; dt_linear ~48×48 < gate) → MC-2's value is the **②-bringup foundation**, not a speedup.
  - **MC-2.0 SHIPPED + AUDIT MERGEABLE 2026-06-20 (b5bacd89):** wake ONE parked aarch64 secondary via PSCI CPU_ON, run a deterministic tile, the kernel SURVIVES it (boots to shell, dispatcher byte-for-byte untouched). Cert `[mc2-boot-survives]`, 4-way falsifiable; faulting-tile bounded-join watchdog. 5-dim adversarial workflow audit, no blockers.
  - **MC-2.1a SHIPPED + AUDIT MERGEABLE 2026-06-20 (bba18e19):** N secondary cores + a real work-queue + `[mc2-smp-equiv]` BYTE-IDENTITY across {1,2,4} (FNV `0xd4c96f986cbafb17` all three). Audit PROVED parallelism real (cores 2,3 ran distinct ranges), found+fixed 2 bugs (join-on-nhelpers race, double-selftest). Tooth A bites; Tooth B (barriers/SMPEN) honestly deferred to RPi3. `mc2-1-ncore-equiv-plan.md`.
  - **MC-2.1b SHIPPED + AUDIT MERGEABLE 2026-06-20 (0ab9e548):** standalone pk_slice_bm partition unit-check (31 cases, DISJOINT+TOTAL/ORDER/MATCHES-GOLDEN) + falsifier; the golden ref proven independent (not circular). Default build byte-identical. **The ③ multicore arc is COMPLETE + audited end-to-end.**
  - **REMAINING:** **MC-2.2** RPi3 hardware `[live]` (the ONLY place the barrier/SMPEN Tooth B bites) · then **② full SMP** (★mk_pino's end goal: per-CPU run-queues + task migration + cross-CPU `knl_ctxtsk` locking + IPIs, ON TOP of MC-2's bringup/per-CPU/work-queue/lock/barrier foundation) · (later) wire a REAL bare-metal matmul (`dt_linear`) to the seam once SS-7 grows R3 past the gate.

### Thread R — roadmap remainders
- regions R3 width · DNODE_MAX raise · lookup L2/L3 · reflex-deliberation D3 (熟慮の中身) · p-fs P3/P4 · ring3 remainder (dtr-train/lm/dmn/gl into ring3, async 0x240/0x241, x87 FXSAVE, aarch64 EL0) · dproc_kill_by_name teardown debt.

### Thread P — product / hardware
- Play Store public release (UMP/ark Phase D) · aarch64 real hardware (RPi3) + netboot · doc physical-halve + artifact rename (the critique's shrink move; "年輪" growth accepted, so this is light).

### Thread X — EXCAVATED (was designed/decided in memory+md but NOT tracked here; folded in 2026-06-20)
- **WebOS — human computing environment** (`web-os.md`, `project_webos_human_environment`): on TOP of yurikago, a HUMAN-facing Web OS served from the node (responsive: desktop on PC, mobile shell on phone) — the human's window onto the SAME p-fs/shell/mind substrate self-access gives the AI. DISTINCT from imaginary-UI (AI-conjured GUIs). Design-doc-first, multi-wave. **[big, untracked]**
- **LM scale wall — a real conversational model** (`project_next_phase_vision` bucket 2; surveys done: `base-model-survey.md`, `conversation.md`, `moe-distillation-survey.md`): scale the byte student toward a genuinely capable conversational mind (beyond watch-class M). SS-7 (bigger baby) is one lever; the broader strategy is bigger. **[strategic]**
- **device-capacity — 端末性能に応じたサイズ** (mk_pino's explicit "measure the device + auto-fit the mind", the SMP-AUTODETECT mind-sizing sibling). **SHIPPED + audit MERGEABLE-WITH-NITS 2026-06-22 (merge `66201a25`, comment-nit fix `17b78c3e`; design `295118b8`):** each node measures RAM+cores at boot and auto-fits its student tier; auditor falsified hard (4000-input-style adversarial: clamp probed over caps {-5..1M}, falsifier OOMs even at 128MB where M fits). M=`63e8de33`/L=`67f2434f` UNMOVED, **S newly PINNED+ASSERTED `0a5bf44c`** (`student_devfit_test.c:292`), bare-metal `.text` 755a20fa + tier-pick symbols absent (hosted-only). The S/M/L student tier machinery ALREADY EXISTS (`st_init_tier`/`ST_TIERS`/heap arena, no VLA) — the only gap is a boot-time `tier = tier_of(ram,cores)` caller (production boots M everywhere). Size knob = the DISCRETE tier (not a continuous score → keeps the `(tier,nexpert)` cohort key + dodges VLA); `R_DM` is explicitly NOT the lever (crown dimension, breaks gl_merge R_NP guard). `tier_of`: RAM-bottleneck min, thresholds from per-tier arena cost, cores-only fallback on low-trust bare-metal, alloc-fail L→M→S step-down (a lying RAM degrades, never OOMs). **Reconciles SS-4 via `[device-ceiling-fleet-grows-within]`:** device picks tier = local expert ceiling `ST_E_<tier>` (S2/M4/L8); SS-4's `cap_experts_of(N)` grows nexpert WITHIN it (`min(cap_experts_of(N), ST_E_<tier>)`) → resolves SS-4 open-risk #7, they hard-`min` never fight. cert `[device-fit]` (same binary + injected profiles → matching tier, no OOM) + falsifier `-DDEVFIT_IGNORE_MEASURE` (hardcode L → 512MB OOM). CROWN: R3 crown untouched (no size axis); per-tier student-forward-hash FAMILY (M=`63e8de33`, L=`67f2434f`, **S=TBD — must be pinned before S is production-selectable**); M default byte-identical. Biggest risk = M-path stays byte-identical after the sizing wrapper (re-prove M-hash + R3 crown or STOP). Honest caveat: fragments the fleet into ≤3 cohorts; the cross-cohort distillation bridge is unverified (this slice = tier-pick + cohort isolation + R3-crown share, NOT solved distillation). IMPL queued (small; student.c, SERIAL-free now SS-4 merged).
- **GPU** (`gpu-compute.md`: Vulkan matmul backend SHIPPED, integration pre-impl · `gpu-3-wiring.md`: design+critique done, **DEFER**): resource-aware GPU acceleration. Partly done.
- **ark app UX queue** (`feedback_ump_ux_principles`): pop picture-rich 3-4 page intro (the big one) · page-chrome i18n for the other ~30 langs · key-derived unique node id · on-device salty-cert harness · MainActivity auto-path log-drain.
- **r3-model-widening** (`r3-model-widening.md`, design only) — likely == Thread R "regions R3 width".
- principle (not a task): **「いいねのない銀河」** (`project_recognition_layers`) — UX rule: feedback from the SYSTEM yes, human-vs-human comparison no.

> **PRIORITY NOTE (2026-06-14 critique steer):** "more features/LM < foundation + **federation** + honest finitude." The foundation crack (KILL-CHURN #PF) is CURED (wave-56); the live remaining steer is **federation** (254→10k) — hence the federation-R0 design-harden in flight. Keep the dream-tier names + co-located honest labels; keep `[live]`/`[in-proc]` mechanical tags.

## DOC-STATUS FIXES — ✅ DONE (2026-06-19): all 7 docs (galaxy.md, ark-profile.md,
signing.md, persistence.md, living-mind.md, inference-engine.md, living-body-inspector.md)
already carry corrected "SHIPPED … doc-status fix" Status lines. Verified 2026-06-21.

## RULES (constitution)
Commander orchestrates only; implementer ≠ auditor ≠ commander; background + yield (never sit
idle on one agent); the audit is the immune system; M-tier/determinism/one-mind/NOCENTRAL
invariants are non-negotiable; one shrinking ledger.
