<?xml version="1.0" encoding="utf-8"?>
<!-- Hero "Shine" button — galaxy.html .primary style: dark blue fill, blue
     border, rounded. The one button a first-time user needs to tap. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/hero_fill"/>
    <stroke android:width="1dp" android:color="@color/star_blue"/>
    <corners android:radius="14dp"/>
</shape>
