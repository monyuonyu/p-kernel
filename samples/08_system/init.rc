# p-kernel /etc/init.rc
# 起動時に自動実行されるスクリプト
# shell_task の run_initrc() から読み込まれる

mkdir /etc
mkdir /shared

# kloader_task.c (カーネル空間) が OTA を担当するため kloader_d.elf は不要
# guard /kloader_d.elf

# Phase 12: 分散 Transformer 推論デーモン
guard /infer_d.elf
